PORT=4000

# Deixe como está
IA_SERVICE_KEY=key-9b8cb1c9e5b8f0a4c3d9e7c8e2f1-s-IA

# Redis (usado pelo KeyStateStore — rotação inteligente de chaves de IA).
# Em dev local, sobe via docker-compose do back.
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# OpenRouter referer (opcional, recomendado pelos termos deles).
OPENROUTER_REFERER=http://localhost:3000
