# CLAUDE.md — Pub Mail (operação + evolução)

Plataforma multi-tenant de **webchats com IA** + **e-mail marketing**, com auto-provisionamento de domínios de cliente (HTTPS via certbot). Stack: NestJS (back) + 2 micros Express (ai, email) + SPA React/Vite (front) + MySQL + Redis.

> Para o panorama geral do produto e do código, veja `README.md`. Este arquivo foca em **como o ambiente de produção está montado, como fazer deploy, e o que já evoluímos**.

> ⚠️ **AO INICIAR / QUANDO O CONTEXTO RECARREGAR:** antes de começar qualquer task, **escaneie este `CLAUDE.md` inteiro + a memória (`~/.claude/.../memory/` via `MEMORY.md`) + o código relevante do projeto** (grep/leitura dos módulos que a task toca). Não confie só no resumo — a maior parte dos erros vem de agir sem reler os padrões (deploy via SSM, gotchas de config, timezone BRT, normalização de anúncios, contrato do micro de e-mail, etc.). Reler custa pouco e evita retrabalho.

---

## ⚠️ MIGRAÇÃO DE SERVIDOR (set/2026 — LEIA PRIMEIRO)
- A instalação na **AWS está sendo DESATIVADA** pelo time de DevOps. A credencial `blue` foi invalidada (chave `…H4R2` — provável quarentena automática da AWS após eu ecoar URLs pré-assinadas com o Access Key ID no terminal). **Não existe mais acesso à instância antiga a partir daqui**; as seções "Produção (AWS)" e "Como fazer DEPLOY" abaixo são **histórico**.
- Próximo ambiente: **servidor novo, banco limpo**. Runbook oficial de instalação do zero: **`README.md` → "Deploy em produção"** (7 passos + seed). Resumo do que o código já garante:
  - `yarn install` reprodutível: `.yarnrc.yml` com `nodeLinker: node-modules` em `back/`, `front/vite/`, `ai-micro-services/`, `email-micro-services/` (Yarn 4 default é PnP e quebra Prisma).
  - `yarn prisma migrate deploy` roda a cadeia inteira num banco vazio (validado: as migrations `20260924120000_org_settings` e `20260924130000_org_settings_cleanup` são idempotentes/no-op em banco novo — o seed por id de org antigo não casa nada; o `DROP COLUMN` das chaves legadas encontra as colunas criadas pelas migrations de abr/jul).
  - **Seed de plataforma** `back/scripts/seed-platform.mjs` (gera SQL idempotente; tudo por env: `SEED_ADMIN_EMAIL/PASSWORD/ORG_NAME/ADMIN_NAME/EDGE_IP/CERTBOT_EMAIL`) → `system_settings` (só infra) + org oculta (`hidden=1`) + usuário `SUPER_ADMIN` com bcrypt 12. **Sem senha no código.**
  - **Isolamento por org garantido**: `organization_settings` (chaves de IA/Resend/webhook por org); `system_settings` só infra e só SUPER_ADMIN edita; webhook do Resend e supressão por org; auditoria sistemática das 202 queries por `id` em modelos com `organization_id` — todas precedidas de checagem de org (set/2026).
  - **Typecheck local funciona**: `cd back && yarn install && yarn prisma generate && yarn tsc --noEmit` (0 erros na entrega de set/2026). `back/yarn.lock` está versionado.
  - Pós-subida: reapontar webhooks (Telegram = revalidar bots; Resend por org; gateways PIX), cadastrar chaves por organização em Integrações, `VITE_API_URL`/`PUBLIC_API_URL`/`BASE_URL_API`/`NEST_WEBHOOK_URL` com o domínio novo.
- **Higiene do repositório (set/2026):** o repo NÃO tinha `.gitignore` e versionava `back/.env`, `ai-micro-services/.env`, `email-micro-services/.env` (segredos!), `back/dist`, `front/vite/dist`, `back/uploads`, `back/generated`. Criado `.gitignore` na raiz e tudo isso saiu do índice (`git rm --cached`; os arquivos seguem no disco). **Os segredos antigos continuam no histórico do git** → no servidor novo usar segredos NOVOS (JWT, chaves dos micros, senha do banco) — nunca reaproveitar os da AWS. Templates: `*/.env.example` (só nomes/defaults). `back/generated` sai do `yarn prisma generate` (está no runbook).
- **Regra permanente:** nunca imprimir URL pré-assinada/credencial no terminal ou no chat (mascarar). Nunca deployar arquivo sem comparar com a versão do servidor. Nunca reiniciar após `nest build` com `error TS`.

## Arquitetura (resumo)

| Serviço | Stack | Porta | Exposto publicamente? |
|---|---|---|---|
| `back` | NestJS 11 / Prisma 7 (adapter MariaDB) | 8000 | ✅ via `api.bluewebchat.online` |
| `front/vite` | React 19 / Vite 7 (SPA estática) | — (nginx serve `dist/`) | ✅ via `bluewebchat.online` + domínios de cliente |
| `ai-micro-services` | Express ESM (ioredis) | 4000 | ❌ só loopback |
| `email-micro-services` | Express ESM (BullMQ) | 9999 | ❌ só loopback (sem auth no `/send`) |
| MySQL 8 / Redis 7 | infra | 3306 / 6379 | ❌ só 127.0.0.1 |

- O **front (browser) só fala com o back** (`VITE_API_URL`). Os micros são chamados pelo back por `127.0.0.1`. **Não** existem subdomínios `ia.`/`email.` (e não devem — o email-micro não tem auth).
- Webchat público é servido em `https://<dominio-do-cliente>/webchat/<slug>` (mesma SPA, resolve por `window.location.hostname` + slug).

---

## Produção (AWS)

- **EC2** `ec2-webchat` `i-06b82885d4f2df991` · região **us-east-2** · Ubuntu 24.04 · IP **3.15.235.224**.
- **Acesso: SOMENTE via AWS SSM** (sem SSH). Perfil aws `blue` (conta 616745996127). O `session-manager-plugin` não está instalado local → use **`aws ssm send-command`** (RunShellScript) + `get-command-invocation`.
  - O SSM roda em **`/bin/sh` (dash)** — sem `trap ERR`, sem bashismos. Heredoc multi-elemento ok.
- **Security Group** `sg-0c492d725ae1f9e26`: só **80/443** abertos.
- **Domínios** (Cloudflare, **DNS only / cinza**): `bluewebchat.online` → front (apex), `api.bluewebchat.online` → back. SSL via certbot (renova sozinho, `certbot.timer`).
- **Domínios de cliente**: cliente aponta A record para **3.15.235.224** (nuvem **laranja** ok — o back aceita CIDRs do Cloudflare). Clicar "Verificar DNS" gera vhost nginx + roda certbot automaticamente.
- Código em **`/opt/pub-mail`** (deploy nativo, sem Docker). Segredos em **`/root/pubmail.secrets`** (root-only): `DB_PASS`, `JWT_ACCESS_SECRET`, `JWT_REFRESH_SECRET`, `IA_SERVICE_KEY`, `EMAIL_SERVICE_KEY`.
- Serviços systemd: `pubmail-back` (:8000, roda como **root** p/ escrever vhost nginx + rodar certbot), `pubmail-ai` (:4000), `pubmail-email` (:9999).
- MySQL: DB **`pubmail`**, user **`pubmail`** (mysql_native_password, TCP 127.0.0.1).

### Hardening p/ tráfego (aplicado — jul/2026)
EC2 = **15GB RAM / 4 vCPU / 77GB disco** (folga grande). Ajustes feitos p/ aguentar disparo em massa:
- **Redis:** `maxmemory 3gb` + `noeviction` (nunca evicta job — se encher, falha alto em vez de OOM). BullMQ job carrega o array de leads inteiro no payload.
- **Micro de e-mail:** `Worker concurrency=1` **de propósito** (o rate-limit da Resend, 550ms entre lotes de 100, é global — paralelismo estouraria 429). Jobs com `attempts:3`+backoff e `removeOnComplete:200/removeOnFail:1000` (antes acumulavam pra sempre no Redis). Retry é seguro: o worker trata erro por-lote, não relança após envio parcial.
- **MySQL:** `innodb_buffer_pool_size` 128MB→**4GB** (`SET PERSIST`, online). `max_connections=151`, pool ~50-60 em uso.
- **Swap:** 2GB (cushion de OOM; não havia).
- **Gargalo conhecido p/ escala futura:** back é 1 processo Node (1 core sob carga) — quiz/API pesados se beneficiariam de cluster (PM2/Node cluster) nos 4 cores. nginx `worker_connections 768`×4.

### Gotchas de config (IMPORTANTES — onde o README erra)
1. **Runtime do banco usa `DATABASE_HOST/USER/PASS/NAME/PORT`**, NÃO `DATABASE_URL` (`back/src/prisma/prisma.service.ts`). `DATABASE_URL` só serve ao CLI do Prisma (migrate/generate). Os dois coexistem no `back/.env`.
2. **`DATABASE_POOL_LIMIT=10`** é obrigatório no `back/.env`. O adapter MariaDB mantém `minimumIdle = connectionLimit` e o back abre ~5 pools; com o default (50) ele quer 250 conexões e estoura o `max_connections=151` ("Too many connections"). Com 10 → 50 conexões estáveis.
3. **`WEBCHAT_BACKEND_INTERNAL_URL=http://127.0.0.1:8000`** (default do código é `:3000`; sem isso o `/ads.txt` dos domínios de cliente quebra).
4. Micros em loopback no `back/.env`: `IA_SERVICE_URL=http://127.0.0.1:4000`, `EMAIL_SERVICE_URL=http://127.0.0.1:9999`. email-micro precisa de `NEST_WEBHOOK_URL` (callback interno) e `BASE_URL_API=https://api.bluewebchat.online` (pixel/tracking público).
5. **ffmpeg/ffprobe** é dependência (upload de vídeo em posts). **`sharp`** é dependência do back (otimização de imagem do e-mail em `email-marketing/assets`) — instalado no server (vips 8.15.3) e no `back/package.json`; é opcional em runtime (fallback sem otimizar). **nginx serve `/uploads` direto do disco** (bloco `location ^~ /uploads/ { alias /opt/pub-mail/back/uploads/; expires 365d; add_header Cache-Control "public, immutable"; }` no vhost `pubmail-api`, antes do `location /`) — imagens do e-mail não passam pelo Node.
6. `system_settings` (singleton id=1) guarda `webchat_edge_ip` (= `3.15.235.224`) e `certbot_email` — já semeados. Sem isso, provisionamento de domínio de cliente falha.
7. Front no build precisa de `VITE_APP_BASE_NAME=/` e `VITE_API_URL=https://api.bluewebchat.online`.
8. **Timezone + horários quebrados:** servidor e MySQL rodam em **UTC**. O usuário escolhe a hora do agendamento de e-mail em **Brasília**. O `EmailSchedule.decide` (`back/src/cron-jobs/domain/email_schedules_domain.ts`) compara **minuto-do-dia** (0..1439) em **`America/Sao_Paulo`** via `Intl.DateTimeFormat` (não usa `getHours/getMinutes`, que dariam UTC). **`time` no banco = minuto-do-dia (0..1439)** — suporta horários "quebrados" (ex.: 11:30 = 690). Migration `..._schedule_time_minute_of_day` converteu os valores legados de hora (0..23) para minuto-do-dia (×60), em `email_projects_schedules.time` e `email_projects_schedules_sent.schedule_time`. O **cron tica a cada minuto** (`@Cron('0 * * * * *')`), com `run_at` em precisão de minuto (idempotência via `@@unique(project_id, schedule_id, run_at)`). Front usa `<TextField type="time">` e envia `time` = hora×60+minuto; exibe HH:MM via helper `minToHHMM`. `date` (one-off) = meia-noite UTC do dia escolhido. **Disparar agora** (`executeManualDispatch(projectId, templateId?)`) aceita template específico ou aleatório. **Reenvio para não-abridores** (`resendToUnopeners(org, projectId, sentId, subject?)` no runner; endpoint `POST /email/projects/:id/schedules-sent/:sentId/resend-unopened` + `GET .../unopened-count`): reusa o `body_html` do disparo original e reenvia só para inscritos ativos que NÃO estão em `email_schedules_sent_opens` daquele disparo (util `getUnopeners`), com assunto opcional novo. Front: ícone "Reenviar p/ não-abridores" (`ForwardToInboxRoundedIcon`) em cada card de Envios (`Sents.jsx`) → dialog com contagem ao vivo + assunto editável.

---

## Como fazer DEPLOY de alterações

Como não há SSH, o fluxo é: empacotar arquivos → S3 → URL assinada → baixar/extrair na instância via SSM → buildar → trocar atômico / reiniciar.

**Bucket de deploy:** `s3://pubmail-deploy-616745996127` (us-east-2).

### Front (mudou só `.jsx`/`.js` do front)
1. Local: `tar -czf /tmp/x.tgz front/vite/src/.../Arquivo.jsx ...` (caminhos relativos à raiz do repo).
2. `aws s3 cp /tmp/x.tgz s3://pubmail-deploy-616745996127/x.tgz --region us-east-2 --profile blue` e gerar `aws s3 presign ... --expires-in 1800`.
3. Via SSM, na instância: `curl -fsSL -o /tmp/x.tgz "<URL>"` → `tar -xzf /tmp/x.tgz -C /opt/pub-mail` → `cd /opt/pub-mail/front/vite` → `export NODE_OPTIONS=--max-old-space-size=4096` → **build em temp + swap atômico** (zero downtime, e se falhar o site fica intacto):
   ```sh
   rm -rf dist_new
   yarn build --outDir dist_new
   [ -f dist_new/index.html ] && { rm -rf dist_prev; mv dist dist_prev; mv dist_new dist; }
   ```
4. O usuário precisa de **hard refresh** (`Cmd+Shift+R`) — o hash do bundle muda.

### Back (mudou `.ts` do back)
1–3. Mesmo upload/extract. Depois, em `/opt/pub-mail/back`:
   ```sh
   # só se mudou schema.prisma / nova migration:
   yarn prisma migrate deploy
   yarn prisma generate
   yarn nest build              # gera dist/src/main.js (processo em memória segue de pé)
   systemctl restart pubmail-back
   ```
   Só reinicie se o build passou (`test -f dist/src/main.js`). Migrations: criar pasta `back/prisma/migrations/<timestamp>_nome/migration.sql` (timestamp > último; ex.: `20260616...`) + atualizar `schema.prisma`.

### Padrão de SSM (template)
```sh
CMD_ID=$(aws ssm send-command --region us-east-2 --profile blue \
  --instance-ids i-06b82885d4f2df991 --document-name "AWS-RunShellScript" \
  --timeout-seconds 1800 --parameters 'commands=["..."]' \
  --query 'Command.CommandId' --output text)
# poll get-command-invocation até Status != InProgress
```
SQL ad-hoc no banco: jogar o SQL num arquivo (base64 → decode na instância) e `mysql -upubmail -p"$(grep ^DB_PASS= /root/pubmail.secrets|cut -d= -f2)" -h127.0.0.1 pubmail < arquivo.sql` (evita inferno de aspas).

---

## Permissões / Roles

Enum `users_role`: **OWNER, ADMIN, MEMBER, SUPER_ADMIN**. O role vai no **JWT** (`req.user.role`) e no `/auth/me` (front lê em **`user?.user?.role`** — objeto aninhado `{user, organization}`).
- **SUPER_ADMIN** = admin de plataforma (vê/gerencia todas as orgs). Criado nesta evolução. **Trocar role exige relogar** (o token carrega o role antigo até expirar/refresh).
- Contas SUPER_ADMIN atuais: `santiago@bluewebchat.online`, `gabriel@bluegridmedia.com`.
- **Conta OCULTA do desenvolvedor (set/2026):** coluna `organizations.hidden` (migration `20260921120000_add_org_hidden`). Org `SuperAdmin` (`hidden=1`) + usuário `superadmin@pubmail.com` (SUPER_ADMIN; senha só com o dono — trocar via SQL/bcrypt 12 ou pela própria conta). `AdminService` (`seesHidden`/`assertOrgVisible`, recebe `req.user.organizationId` do controller): org oculta e seus usuários são **inexistentes** na API admin pra quem é de fora — somem de `GET /admin/organizations|users` e dão 404 em editar/trocar senha/apagar/criar usuário nela; só quem é da própria org oculta enxerga/gerencia. Dados isolados pelo scoping normal de `organization_id`. **Qualquer novo endpoint cross-org (admin/analytics globais) DEVE filtrar `hidden:false`.**
- **Chaves de API por organização (set/2026 — corrigiu vazamento cross-tenant):** `system_settings` (singleton `id=1`) era compartilhado por TODAS as orgs (chaves de IA, Resend, webhook secret). Agora: tabela **`organization_settings`** (`organization_id` PK/FK CASCADE; migration `20260924120000_org_settings`, seed das chaves antigas SÓ p/ Blue Grid Media — migration `20260924130000_org_settings_cleanup` removeu o seed indevido da org oculta e **DROPOU** as colunas legadas de chaves do `system_settings`; a org `ronal` começou vazia) com groq/cerebras/gemini/mistral/openrouter/sambanova/resend_api_key/resend_webhook_secret. `system_settings` ficou **só com infra da plataforma** (`webchat_edge_ip`, `certbot_email`; colunas antigas de chaves ficaram no banco, sem uso). `SystemSettingsService`: todo método recebe `organizationId` (`get/update/addAiKeys/removeAiKey/getAiKeysStatus/getGroqApiKeysOrFail/getAiProviderKeysOrFail/getResendApiKeyOrFail/getResendWebhookSecret`); `update(org, dto, canEditInfra)` só grava edge/certbot se `req.user.role==='SUPER_ADMIN'`; resposta devolve `can_edit_infra` (front `IntegrationsManager` esconde o card Edge/SSL e não envia esses campos). Consumidores passam a org do contexto (`project.organization_id`, `webchat.organization_id`, `payload.organization.id`). **`resend-webhooks.service`** resolve a org pelo `dispatch_id` (sent row) ANTES de verificar a assinatura (segredo daquela org) e a **supressão por bounce/spam agora é por org** (era global por e-mail). `admin.removeOrganization` apaga `organization_settings` (FK-check está off ali — cascade não roda). E2E validado com org temporária via `/admin/organizations` (criar → login → GET vazio → add key → PATCH infra ignorado → superadmin intacto → apagar).
- Guard: `@UseGuards(JwtAuthGuard, RolesGuard)` + `@Roles('SUPER_ADMIN')` (`back/src/auth/roles.guard.ts`, `roles.decorator.ts`).

---

## Evolução já feita (histórico desta jornada)

### Deploy inicial (AWS)
Varredura completa do sistema → provisionamento da EC2 → pacotes (node20, yarn4, nginx, mysql8, redis7, certbot, ffmpeg) → MySQL/Redis → upload do código via S3 → 4 `.env` de produção (com as correções dos gotchas acima) → build → systemd (back/ai/email) → nginx vhosts (`pubmail-front`, `pubmail-api`, `000-default-deny`) → certbot SSL nos 2 domínios → seed `system_settings` → fix do pool de conexões.

### Webchat — builder de funil
- **Bug de arrastar bloco** (sumia/tela branca): era re-render a cada frame de drag. Corrigido com `useNodesState` + persistir posição só no `onNodeDragStop` (`front/vite/src/views/pages/webchat/FunnelBuilder.jsx`).

### Webchat — botões (antes "opções/respostas rápidas")
- **Renomeado para "Botões"** em todo o UI (builder visual + funil), mantendo as chaves de dados (`quickReplies`, `ending.options`, `value="quick"/"options"`).
- **Visual padrão**: largura total, empilhados, texto centralizado bold 600, cantos pill. Tema WhatsApp → fundo verde + texto branco. Customização preservada (`quickReplyBgColor`).
- **Classes CSS customizadas por botão** (campo "Classes CSS" no builder visual e no funil; sanitizado; aplicado no `class=""` do botão público). Campo `cssClass` em `quickReplies[]` e em `ending.options[]`.

### Webchat — layout responsivo (público)
- **Desktop (≥768px)**: chat vira **coluna centralizada de 720px**; as laterais usam o **mesmo fundo/wallpaper** do chat (contínuo). Bolha do bot ≥52% da coluna, fonte +2px.
- **Mobile (<768px)**: bolhas como antes, mas **botões ocupam 100% do container** do chat.
- Implementado com `<style>` + media queries em `PublicWebchat.jsx` (classes `pubmail-shell`, `pubmail-msg-wrap`, `pubmail-bot-bubble`, `pubmail-qr`, `pubmail-qr-btn`).

### Webchat — outros ajustes
- **Mensagem final do funil vazia** não gera mais bolha em branco (detecta HTML vazio via texto puro, não só `.trim()`).
- **Imagens** preenchem todo o container da mensagem (`width:100%`).
- **Páginas Legais**: nova aba no builder visual (label + link, reordenável). Renderiza um **rodapé discreto** no fim do webchat (público + preview). Persiste em `personalizacao.legalPages` (JSON livre — sem mudança no back).

### Anúncios — rótulo opcional
- Toggle **"Exibir rótulo de publicidade"** (antes o rótulo "PUBLICIDADE" voltava sempre). Causa real: o save nem persistia `rotulo`, e a tabela `webchat_ads` não tinha coluna pra flag.
- **Coluna `rotulo_ativo`** adicionada (migration) + DTO (`update-webchat-ads.dto.ts`) + service (`webchat.service.ts` escrita/leitura) + payload do front (`Webchats.jsx` `buildAdsPayloadFromConfig`). Render respeita `rotuloAtivo===false` (`PublicWebchat.jsx` `resolveAdRotulo`).

### Splits de webchats (pastas + redirecionamento ponderado)
- **Modelo**: tabela `webchat_splits` (slug **único global**, `is_default`), `webchats.split_id` + `webchats.split_weight` (peso relativo, default 100). Todo webchat pertence a um split; default "Padrão"/`padrao`.
- **Back**: módulo `webchat-splits` (CRUD org-scoped + `GET /public/webchat-splits/:slug` público com membros/pesos). `webchat.service` resolve split no create/update e inclui `split { slug, members:[{domain,slug,weight}] }` no config público. Apagar split: bloqueado se não-vazio; padrão não apaga.
- **Front**: página Webchats com **abas = splits** (criar/renomear/apagar/copiar-link), input de **peso %** por webchat (autosave no blur/Enter), webchat novo entra na aba ativa. Rota pública `/webchat/splits/:slug` (`SplitRedirect.jsx`) sorteia e redireciona.
- **Comportamento**: **re-sorteia a cada visita** (sticky NÃO usado, por escolha do usuário). Trava anti-loop: o redirect leva `?nsr=1` que suprime o sorteio naquele pulo e é limpo da URL — evita loop A→B→A. Link do split fica na plataforma (`bluewebchat.online/webchat/splits/<slug>`). Redirect vai para `https://<domínio>/webchat/<slug>`.

### Página Admin de Usuários/Organizações (`/admin/users`, SUPER_ADMIN)
- **Back**: módulo `back/src/admin/*` (`@Roles('SUPER_ADMIN')`). CRUD cross-org de organizações e usuários, **criar org + usuário admin** (transação org+wallet+user), **alterar senha** (bcrypt 12, invalida sessões). **Hard delete**: usuário direto; organização apaga TODO o conteúdo (transação com `FOREIGN_KEY_CHECKS=0` + `try/finally`, cobre as 27 tabelas do tenant).
- **Front**: item de sidebar "Administração → Usuários" (filtrado por role em `MenuList`), guard `RequireAdmin`, rota `/admin/users`, página `views/pages/admin/Users.jsx` (abas Organizações/Usuários, mostra ID da org).

---

### Módulo Quizzes (EM CONSTRUÇÃO — por partes)
Plano: usuário cadastra/verifica domínios de quiz (igual webchat) → cria quizzes (build próprio, mais simples) → pode vincular a um Projeto de e-mail (captação de leads) → no fim do quiz, redireciona para URL (geral ou baseada nas respostas).
- **Parte 1 — domínios de quiz (FEITA):** reusa 100% o provisionamento de domínio do webchat. Adicionado campo **`kind`** ('webchat'|'quiz') em `webchat_domains` (migration `..._add_kind_to_webchat_domains`). `WebchatDomainsService.list/create` recebem `kind`. Novo controller `QuizDomainsController` (`/quiz-domains`, no mesmo módulo) que chama o service com `kind:'quiz'`; verify/remove/ads-txt são por-id (kind-agnósticos). Front: hook `useQuizDomains` (`/quiz-domains`), página `DomainsQuiz.jsx` (cópia de `DomainsWebchat.jsx` com webchat→quiz), nova sub-aba **"Quizzes"** em `DomainsTabs.jsx` (Conta & Domínios). Um domínio pertence a um único kind (slug/domínio único global).
- **Parte 2 — CRUD de quizzes + splits (FEITA):** tabelas `quizzes` (espelho de webchats, SEM agente/anúncios; campos name/slug/domain/email_project_id/header_scripts/footer_scripts/settings/active/split_id/split_weight) e `quiz_splits` (espelho de webchat_splits) — migration `..._add_quizzes`. Back: módulos `quizzes` (CRUD, valida domínio de quiz **VERIFIED**, projeto de e-mail, slug único por domínio) e `quiz-splits` (CRUD + `GET /public/quiz-splits/:slug`). Admin `removeOrganization` agora apaga `quizzes`+`quiz_splits`+`webchat_splits` também. Front: grupo de sidebar **"Quizzes"** → `/quizzes` (`views/pages/quiz/Quizzes.jsx`) com abas=splits, peso %, criar/editar (Nome, Domínio verificado, Projeto de e-mail, header/footer scripts), copiar/abrir link, sem ações de anúncio/builder/avançado. Rota pública `/quiz/splits/:slug` (`QuizSplitRedirect.jsx`) sorteia e redireciona p/ `https://<domínio>/quiz/<slug>`. Hooks `useQuizzes`, `useQuizDomains`.
- **Parte 3 — página pública + builder visual + captação de lead (FEITA):**
  - **Config (JSON livre):** todo o build do quiz vive em `quizzes.settings` (objeto único). Shape + normalização + 3 presets em `front/.../quiz/quizConfig.js` (`normalizeQuizConfig`, `createDefaultQuizConfig`, `QUIZ_PRESETS`, `buildPresetConfig`). Campos: theme (cores/gradiente/card/botões/fonte), logo, image, title, questions[{text, **description**+descriptionPosition/Color/Size/Align/Bold, options[{label, redirect, **cssClass**}], **content[]** (blocos abaixo dos botões: text/heading, image, divider — `createContentBlock`/`normalizeContent`; render em `QuizView.renderContentBlock`)}], onlineIndicator, leadCapture (nome/email/telefone com rótulos editáveis + introText + buttonLabel + **buttonCssClass**), redirect.url, legal{text,pages[]}, animation{type,duration}. **Texto descritivo por pergunta** (acima/abaixo, default não-negrito/centralizado, personalizável) e **classes CSS por botão** (opções + botão de captação) — `sanitizeCssClasses` (só `[a-zA-Z0-9_-]`+espaço) aplicado no `class=""` no `QuizView`. Tudo em `settings` ⇒ sem mudança no back.
  - **Renderer único `QuizView.jsx`** (usado pela página pública E pelo preview do builder → preview = real). Centralizado vertical+horizontal, transições (slide/fade/zoom), fluxo perguntas→captação→redirect.
  - **Página pública `PublicQuiz.jsx`** em `/quiz/:slug` (PublicRoutes, resolve por `?domain=`/hostname). Injeta `header_scripts`/`footer_scripts` recriando `<script>`. Captura → `POST /public/quiz/:slug/leads`.
  - **Builder `QuizBuilder.jsx`** em `/quizzes/:id/builder` (ação "Builder visual" na lista). Preview ao vivo, **import/export de preset (JSON)**, upload de imagem→data URL (sem backend), autosave (debounce 2s) + Salvar. Presets: **Clean** (print), **Vibrante**, **Dark Neon**.
  - **Back:** tabela `quiz_leads` (migration `..._add_quiz_leads`). Públicos `GET /public/quiz/:slug/config` e `POST /public/quiz/:slug/leads` (`public-quizzes.controller`). `captureLead` SEMPRE grava `quiz_leads` e, se quiz tem `email_project_id`+e-mail, roteia pro `email_leads`+`email_project_leads` (igual webchat). `GET /quiz-leads` (+`/export`) p/ a página Leads.
  - **Leads (`webchat/Leads.jsx`):** seletor **Origem: Webchats | Quizzes | Projetos de e-mail** → busca `/webchat-leads` | `/quiz-leads` | `/email-project-leads`. Sem e-mail/sem projeto, o lead fica só em `quiz_leads` (visível em Leads). E-mail é o que roteia pro projeto. A origem **Projetos de e-mail** (`email-marketing/project-leads`, endpoint `GET /email-project-leads?project_id&page&q` + `/export`) lista a pivô `email_project_leads`+`email_leads` do projeto → inclui os leads do **formulário/API nativo** E os **roteados de quizzes/webchats vinculados** (source vem de `attributes.source`; phone de `attributes.phone`).
- **Parte 6 — e-mail imediato ao lead (FEITA):** colunas `lead_email_html` (LONGTEXT) + `lead_email_subject` no `quizzes` (migration `..._add_quiz_lead_email`). No modal de criar/editar quiz, **se há projeto de e-mail vinculado**, aparece o campo com abas **HTML / Preview** (preview num `<iframe sandbox srcDoc>`). Variáveis Handlebars: `{{name}}/{{email}}/{{phone}}/{{unsubscribe_link}}`. **`QuizLeadEmailService.maybeSend`** (best-effort, NUNCA lança) é chamado no fim do `captureLead`: se houver `lead_email_html` + projeto + e-mail do lead, monta payload de 1 lead e chama o micro `/send` (reusa o disparo, cria `email_projects_schedules_sent` p/ histórico/tracking, cobra token igual disparo). Sem HTML ⇒ não envia. Requer `project.settings.sender` configurado.
- **Parte 5 — anúncios (FEITA, espelhado do webchat):**
  - **Modelo:** tabela `quiz_ads` (espelho de `webchat_ads`, com `quiz_id`) — migration `..._add_quiz_ads`. Uma linha por **posição** (`@@unique(quiz_id, position)`). Por ora a UI só expõe **`topo`**, mas o back já aceita `rodape`/`intersticial` (DTO `update-quiz-ads.dto.ts` + `quiz-ads.service`).
  - **Normalização:** `back/src/quizzes/ads-normalize.util.ts` = cópia FIEL das funções puras do webchat (`normalizeAdContract` etc.) — infere `gpt_slot`/`gpt_div_id`/`gpt_sizes` do código colado, detecta HTML fixo (ADX), preserva rótulo. **NÃO divergir disso** ou o anúncio não imprime.
  - **Back:** `QuizAdsService` (get/update por posição; vazio remove a posição; `ativo=false` preserva mas some do público). Endpoints `GET /quizzes/:id/ads` e `PATCH /quizzes/:id/ads` no `quizzes.controller`. `getPublicConfig` inclui `ads_config` (publicOnly=true → sem inativos). Admin removeOrg apaga `quiz_ads`.
  - **Front:** engine `quiz/quizAds.js` (espelho do `PublicWebchat`: 3 modos — HTML fixo / GPT slot / código-como-HTML — carrega `gpt.js`, recria `<script>`, rótulo). `QuizView` recebe `ads` e monta o **topo** (`#quiz-ad-topo` acima do card) só no modo **live** (`mountQuizAds`). `PublicQuiz` passa `ads_config`. Configuração via **`QuizAdsDialog.jsx`** (ação 📣 "Anúncios" na lista de quizzes): código, tamanho, avançado (gpt_slot/div), rótulo + toggle, ativar/desativar. Builder NÃO mostra ad (igual webchat).
- **Parte 4 — split na entrada direta + redirect por resposta (FEITA):**
  - **Split fire direto:** `getPublicConfig` agora inclui `split{slug,members}` (via `buildPublicSplit`). `PublicQuiz.jsx` re-sorteia na entrada direta em `/quiz/:slug` e redireciona p/ `https://<domínio>/quiz/<slug>?nsr=1` (trava anti-loop `?nsr=1`, igual webchat).
  - **Redirect por resposta:** cada opção tem campo opcional `redirect` (no builder: "↪ Redirecionar"). `QuizView` usa `redirectRef` (último não-vazio vence) com prioridade sobre `redirect.url` geral; vale tanto no fim sem captação quanto após enviar o lead.
- **Parte 7 — rastreio de anúncio / GTM (FEITA, jul/2026):** o quiz não estava sendo rastreável (GTM não pegava cliques de conversão — "todas as páginas" disparava, mas trigger por classe `qz-btn`/custom não). **Causa:** `QuizView` usava `<button>` movido por estado React (não-navegável, não-âncora), ao contrário do **webchat que usa `<a>` reais** (com comentário explícito no `PublicWebchat.jsx` de que NÃO faz preventDefault em link com URL, pra ad scripts interceptarem). **Fix (só front, `QuizView.jsx`):** (1) opções de resposta e botão de captação viraram **`<a role="button">`** (com `href` = destino quando há, mas navegação segue controlada pelo JS via `preventDefault` + handler; comportamento idêntico). Botão de captação mantém um `<button type="submit">` **escondido** no `<form>` pra preservar "Enter envia". (2) Redirect final agora navega via **clique de âncora real** (`navigateTo` cria `<a>` + `.click()`, não `window.location.href`) pra ad scripts interceptarem — igual webchat. (3) **`dataLayer` (belt+suspenders):** eventos `quiz_answer` / `quiz_lead` (conversão) / `quiz_complete` empurrados no `window.dataLayer` (só live) → o especialista cria trigger de **Evento Personalizado** no GTM, sem depender de DOM. **Cosmético junto:** `leadCapture.buttonBgColor` (cor de fundo do botão de captação, vazio = cor do tema) — campo `ColorField` no `QuizBuilder` + aplicado no `QuizView`; vive em `settings` (JSON livre, sem back). **Webchat já era `<a>`** (nunca reclamaram); se precisar, replicar `dataLayer` nele depois.

### Builder visual de e-mail (templates + e-mail do quiz)
Editor visual **por blocos** (estilo Resend), que **continua salvando HTML** (o jeito de HTML cru segue funcionando).
- **Core** `front/.../email/emailBuilderCore.js`: modelo JSON → **HTML mínimo/humano** (só `div/p/a/img/hr`, estilos inline, **SEM tabelas e SEM comentários**, pra cair na caixa de entrada). Blocos: **text/heading, image, button, divider, spacer, card, columns** + `body`. `modelToHtml` injeta `{{open_email_pixel}}`; botão rastreado vira `href="{{base_webhook_cta_click}}?redirectUrl=<encoded>"`. Variáveis: `{{name}}/{{email}}/{{phone}}/{{unsubscribe_link}}/{{base_webhook_cta_click}}`.
  - **Preheader:** `model.preheader` (texto de prévia na inbox) vira `<div>` oculto no topo (`preheaderHtml`, filler com zero-width chars que empurra o conteúdo real pra fora da prévia). Campo em cima no `EmailBuilder`.
  - **From Name por template:** `model.from_name` (opcional). O envio usa `template.from_name || project.settings.sender.fromName` (fallback). O micro (`email-micro-services/service.js`) resolve `effectiveFromName`; no back, todo caminho de envio extrai `(builder_model|lead_email_model|variant.model|step.builder_model)?.from_name` e grava a coluna `email_projects_schedules_sent.from_name` (migration `..._add_from_name`). Caminhos cobertos: cron `schedules.service` (buildPayload + executeManualDispatch + resendToUnopeners), `flows-runner`, `triggers.service`, `ab.service` (`sendToLeads(...,fromName)`), `quiz-lead-email.service`.
  - **Card** (`card`): caixa com ícone/emoji + título + descrição + borda/fundo/cantos/padding (checklist, "atenção"). Ícone à esquerda via `inline-block`. **Colunas** (`columns`): 2 células lado a lado (`width:48%` + spacer 4%, `font-size:0` no wrapper p/ matar whitespace), cada célula `kind:'image'|'text'` — pro cabeçalho logo+selo. **Barra no topo** (`body.barColor`): faixa colorida com cantos superiores arredondados; o card abaixo fica com topo reto pra emendar. **Botão `fullWidth`**: `display:block;text-align:center`. Card/colunas no canvas do builder renderizam via `renderBlockHtml` (export do core) com `dangerouslySetInnerHTML` ⇒ **preview = HTML real**.
- **Reabrir/editar:** o HTML sai **limpo** (sem marcador) — o modelo do builder é guardado em **coluna JSON no banco**: `email_templates.builder_model` e `quizzes.lead_email_model` (migration `..._add_builder_model`). Coluna não-nula = feito no construtor.
- **UI** `front/.../email/EmailBuilder.jsx`: modal fullscreen (rail de blocos + canvas com edição inline contentEditable + painel de estilo **em tema dark** p/ inputs legíveis). Recebe `initialModel` (do banco). `mode='template'` (nome+assunto, salva template) ou `mode='body'` (devolve `{html, model}` — usado no e-mail do quiz).
- **Imagem (pipeline de performance — carregar RÁPIDO no e-mail):** e-mail **NÃO** renderiza data URL (Gmail bloqueia) — upload vai pro back `POST /email/assets` (`email-marketing/assets`), salva em `uploads/email/` e devolve URL pública. Também aceita colar URL. **3 camadas de otimização (jul/2026):**
  1. **`sharp` no upload** (`assets.service.ts` `optimize()`): redimensiona p/ máx **1000×1000** (`fit:inside`), auto-orient, comprime; sem alpha → **JPEG** (q80, mozjpeg, progressive), com alpha → PNG (palette), GIF preservado. `sharp` é **opcional em runtime** (`try require`) — se faltar, salva o original. **Dependência instalada no server** (`yarn add sharp`, vips 8.15.3) e no `back/package.json`.
  2. **nginx serve `/uploads` DIRETO do disco** (`location ^~ /uploads/ { alias /opt/pub-mail/back/uploads/; ... }` no vhost `pubmail-api`, ANTES do `location /` que faz proxy) — imagem **não passa mais pelo Node single-thread** (esse era o gargalo real da lentidão). `main.ts` `useStaticAssets('/uploads', {maxAge:'365d', immutable:true})` segue como fallback.
  3. **Cache 1 ano immutable** (nomes UUID) → 2ª abertura instantânea.
- **Templates (`email/Templates.jsx`):** botão **"Template"** (editor HTML cru de sempre) + **"Construtor"** (novo). Templates feitos no construtor (têm `builder_model`) aparecem como **linha com botão "Editar"** (reabre o construtor) em vez do accordion de HTML. Quiz: **"Abrir construtor visual"** no modal → escreve `lead_email_html` + `lead_email_model`.

### Teste A/B de assunto e template
Sidebar **Email Marketing → Testes A/B** (`/email/ab-tests`, `views/pages/email/AbTests.jsx`). Módulo `email-marketing/ab` (tabelas `email_ab_tests` + `email_ab_variants`, migration `..._add_ab_tests`; FK→email_projects/tests ON DELETE CASCADE no banco). Cada teste tem 2-4 **variantes** (cada uma = template do projeto + assunto opcional que sobrescreve o do template). Ao criar: `EmailAbService.create` valida, pega os inscritos ativos, **embaralha e fatia** `test_percent`% entre as variantes (resto ≥1), pré-checa saldo, **envia cada fatia agora** (reusa o micro /send via `sendToLeads`, cobra token, grava `email_projects_schedules_sent` e guarda `test_sent_id` na variante), salva `test_emails` (usados p/ excluir do envio final) e `decision_at = now + decision_hours`. **`EmailAbRunner`** (`@Cron(EVERY_MINUTE)`) → `decidePending`: para cada teste `testing` com `decision_at<=now`, calcula a métrica de cada variante (`open_count`/`click_unique_count` ÷ `total_leads` do seu `test_sent_id`), escolhe o vencedor (`winner_metric` open|click), **envia o vencedor pro restante** (ativos que não estão em `test_emails`) e marca `completed` + `winner_variant_id`/`winner_sent_id`. Cancelar (`DELETE`) só enquanto `testing`. Métricas por variante calculadas on-the-fly a partir do sent row. Admin removeOrg apaga `email_ab_variants`+`email_ab_tests`.

### Importar templates do Claude (planilha .xlsx/.csv)
Botão **"Importar templates do Claude"** (com a logo laranja da marca) em toda tela que cadastra template: `email/Templates.jsx` (recycle=false) e `RecycleTab.jsx` (recycle=true). Abre modal: **(1)** botão "Copiar prompt para o Claude" (prompt completo em `emailImportCore.js` `buildClaudePrompt()` — explica a filosofia de HTML arcaico/inbox, as variáveis `{{name}}`/`{{email}}`/`{{phone}}`, que CTA-tracking e descadastro são automáticos, foco em conversão, **regras fortes de entregabilidade/anti-Promoções** (nada de "OFERTA/GRÁTIS/CLIQUE AQUI/CAPS", proporção de texto, 1 CTA, tom 1-a-1), e manda o Claude conversar direto pedindo nicho/oferta/links dos CTAs/quantidade) + "Baixar exemplo (.csv)". **(2)** upload do arquivo. **Formato PLANO** (colunas fáceis pro Claude gerar em CSV/xlsx sem JSON): `name, from_name, subject, preheader, heading, intro, image_url, bullets(sep por |), body, cta_text, cta_url, ps, accent_color` (`from_name`/`preheader` opcionais; `rowToTemplate` seta `model.from_name`/`preheader`). `rowToTemplate` converte cada linha no **`builder_model`** do editor (via `createBlock`/`normalizeModel` do `emailBuilderCore`) → gera HTML com o **mesmo `modelToHtml`** do construtor ⇒ fica **editável no construtor** e sai HTML limpo. Import é **client-side**: parseia com `xlsx` (dynamic import, chunk separado — lê xlsx E csv), e faz `POST /email/projects/:id/templates {name,subject,body_html,builder_model,recycle?}` por linha (endpoint reusado, SEM mudança no back). `esc()` escapa `<>&` dos textos (preserva `{{...}}`); CTA vira link rastreado automaticamente; rodapé de descadastro é sempre anexado. Componente reusável `ImportTemplatesButton.jsx` (props projectId/recycle/onImported).

### Segmentação de agendamentos (condições/regras)
Um agendamento pode disparar só para um **segmento** de leads. Coluna `email_projects_schedules.segment` (JSON `{ match:'all'|'any', conditions[] }`; migration `..._add_schedule_segment`). Avaliador puro + fetch em `back/src/email-marketing/segments/segment.util.ts` (`normalizeSegment`, `filterLinksBySegment(prisma, projectId, links, seg, now)`). No cron `buildPayload`, se `scheduleFull.segment`, filtra `effectiveLinks` (compõe via AND com recycle/cold-block). **Condições:** opened_within / not_opened_within / clicked_within / not_clicked_within (via `metrics.last_open/last_click_cta`), never_interacted, **open_rate/open_count** e **click_rate/click_count** {op,percent|count,days} (via tabelas por-lead `email_schedules_sent_opens` e `email_schedules_sent_clicks` ÷ nº de disparos `sent=true` na janela), has_tag / not_has_tag (`email_leads.tags`), source_is (`attributes.source`), joined_within / joined_before (via `email_project_leads.created_at`).
   - **Cliques por-lead (FEITA):** tabela `email_schedules_sent_clicks` (espelho de opens, `@@unique(schedule_sent_id,email)`) + coluna `email_projects_schedules_sent.click_unique_count` (cliques únicos; `click_cta_count` segue = total). `registerCtaClick` grava o clique único (best-effort) e incrementa `click_unique_count` só na 1ª vez. Destrava as condições click_rate/click_count e CTOR real por lead. **Forward-only** (backfill de `click_unique_count`=`click_cta_count` p/ disparos antigos, mas a tabela por-lead só tem cliques a partir do deploy). DTO `segment?` (create/update em `project_schedules`; `normalizeSegment` sanitiza server-side). Preview ao vivo: `POST /email/projects/:id/segment-preview {segment}` → `{count,total}` (`previewSegment`). Front: `email/SegmentBuilder.jsx` (toggle + match all/any + linhas de condição + contagem ao vivo), embutido no dialog de `Schedules.jsx` (semeado via `key`+`value`, emite estado inicial no mount); chip "Segmento · N regras" no card. **Clique por-lead não é contável** (só `last_click_cta` recência) — cliques não têm tabela única por lead; aberturas têm.

### Templates por agendamento
Coluna `email_projects_schedules.template_ids` (JSON array de ids) — migration `..._add_schedule_template_ids`. No form de agendamento (`email/Schedules.jsx`) há um **multi-select** de templates. Se preenchido, o disparo (`cron-jobs/schedules.service.ts` `buildPayload`) **sorteia entre os selecionados** (que ainda existem) e seta `keepTemplates=true` → a trava de auto-apagar (`!keepTemplates && templatesCount>2`) **não roda**. Vazio/null = comportamento atual (sorteia entre TODOS e pode apagar quando >2). `project_schedules.service` valida os ids contra o projeto no create/update.

### Automações — Fluxo Inicial (drip por projeto de e-mail)
Sidebar **Leads → Automações** (`/leads/automations`, `views/pages/leads/Automations.jsx`) com abas (por ora só **Fluxo Inicial**). Lista os projetos de e-mail; cada um tem **"Configurar fluxo"** → `InitialFlowConfig.jsx` (dialog fullscreen com **visualização vertical em balões**: Lead captado → conector com tempo de espera editável → E-mail 1 → … + "Adicionar e-mail"). Cada step = template feito no **`EmailBuilder` (novo `mode='step'`: só assunto)** + `delay_value`/`delay_unit` (min/horas).
- **Modelo:** `email_flows` (1 por projeto, `active`, `activated_at`), `email_flow_steps` (position, delay, subject, body_html, builder_model, **condition**), `email_flow_enrollments` (lead em andamento: `next_step`, `next_send_at`, `status`, **last_sent_id**) — migrations `..._add_email_flows` + `..._add_flow_conditions_metrics`.
- **Back:** `email-marketing/flows` — `GET /email/flows` (resumo), `GET/PUT /email/projects/:id/flow`, **`GET /email/projects/:id/flow/metrics`**. **`EmailFlowsRunner`** roda **a cada minuto** (`@Cron(EVERY_MINUTE)`): (1) **inscreve** leads do projeto captados **após `activated_at`** e ainda sem enrollment (polling em `email_project_leads`, **sem tocar no código de captação**); (2) **envia** os steps devidos (`next_send_at <= now`) reusando o micro `/send`, cria `email_projects_schedules_sent` (com **`flow_step_id`**), cobra token, avança/completa. `activated_at` só reinicia no false→true (não re-inscreve leads antigos). Desativar cancela enrollments em andamento.
- **Condições por passo (FEITA):** cada passo (do 2º em diante) tem `condition` JSON `{ type:'always'|'opened'|'clicked', on_fail:'skip'|'stop' }` — "só envia se abriu/clicou o e-mail ANTERIOR". O runner guarda o sent row do último envio em `enrollment.last_sent_id`; antes de enviar o passo N avalia `conditionMet` (opened = existe linha em `email_schedules_sent_opens` p/ o last_sent_id; clicked = `click_cta_count>0` do last_sent_id, que é de 1 lead). `on_fail=skip` pula o passo (segue pro próximo), `stop` sai do fluxo (`status='stopped'`). Front: `StepCondition` no conector entre balões.
- **Métricas por passo (FEITA):** `getFlowMetrics` agrega os sent rows por `flow_step_id` (sent, delivered, opens=Σopen_count único, clicks=nº com click_cta_count>0, bounces, spam + taxas open/click/ctor/delivery/bounce/complaint) + funil de inscrições (active/completed/stopped/canceled). Front: `MetricsStrip` (chips) em cada balão + resumo no cabeçalho. **Gotcha:** `saveFlow` agora **reconcilia steps por `id`** (update/create/delete) em vez de apagar+recriar — senão os ids mudavam a cada save e as métricas (keyed por flow_step_id) ficavam órfãs. O front reenvia `id` por step; duplicar zera o id.
- **"Apenas leads que interagiram no fluxo" (FEITA):** toggle no form do projeto (`EmailMarketing.jsx`, abaixo do bloqueio de frios) → `email_projects.settings.flow_interacted_only { enabled }`. Os webhooks de open/click marcam `email_project_leads.metrics.flow_interacted=true` quando o sent row tem `flow_step_id`. No cron `buildPayload` (agendamentos normais, não reciclagem), se habilitado, filtra os links por `metrics.flow_interacted===true`. **Forward-only** (só pega interações após o deploy).

### Sidebar (menu)
Grupos em `front/vite/src/menu-items/` (cada arquivo é um grupo; registrados em `index.js` `items[]`). Ordem atual: **Dashboard · Webchat** (Agentes, Webchats) **· Quizzes · Email Marketing** (Projetos) **· Leads · Administração** (Usuários, SUPER_ADMIN) **· Settings**. O item **Leads** ganhou **grupo próprio** (`leads.js`, url ainda `/webchat/leads`) — vai receber features de leads de e-mail. "Ajuda" saiu do Email Marketing. Um item novo = adicionar ao arquivo do grupo (ou criar grupo + registrar no `index.js`); ícones são do `@tabler/icons-react`.

### Roadmap Email — 4 fases (em execução, uma por deploy)
1. **Fundação (FEITA):** métricas de engajamento + tags na tela de Leads. Coluna `email_leads.tags` (JSON array; migration `..._add_lead_tags`) editável via `PATCH /email/leads/:id` (campo `tags` no `UpdateEmailLeadDto`, sanitizado). `GET /email-project-leads` devolve `last_open`/`last_click_cta`/`last_interaction` (da pivô `metrics`) + `engagement` (`clicked`>`opened`>`none`) + `tags`. `Leads.jsx` (origem **Projetos de e-mail**) mostra chip de **Engajamento** (tooltip com datas) + célula de **Tags** (chips + popover add/remove, PATCH otimista).
   - **Filtros por coluna** (server-side, só origem e-mail): `engagement`, `source`, `tag`, `date_from/date_to` no `/email-project-leads` — usando filtros JSON do Prisma no adapter MariaDB (`metrics: { path, not/equals: Prisma.DbNull }`, `attributes: { path:'$.source', equals }`, `tags: { array_contains }`) — **validado em runtime**. `GET /email/leads/tags` (distintas) alimenta o filtro/autocomplete.
   - **Seleção + tag em massa:** checkbox por linha + "selecionar todos da página" → botão **Adicionar tags** → `POST /email/leads/tags/bulk { lead_ids, add }` (append, sem duplicar).
2. **Reciclagem (win-back) + Bloqueio de frios (FEITA):** "frio" avaliado em memória na pivô `email_project_leads.metrics` (`isColdLink(link, 'never'|'inactive', days)` em `back/src/email-marketing/recycle/recycle.service.ts`).
   - **Bloqueio de frios:** config do projeto em `email_projects.settings.cold_block { enabled, days }` (JSON livre, default 30 dias). No `cron-jobs/schedules.service.ts` `buildPayload`, se `!recycle` e `cold_block.enabled` → **exclui** quem não interagiu no período. UI no form do projeto (`EmailMarketing.jsx`): toggle + dias + **contagem ao vivo** (`GET /email/projects/:id/cold-count?criteria=inactive&days=`).
   - **Reciclagem = agendamentos de verdade:** REUSA a tabela `email_projects_schedules` com colunas novas `recycle`/`recycle_criteria`/`recycle_days` (migration `..._add_recycle_schedules`; +`recycle` em `email_projects_schedules_sent`). Um agendamento `recycle=true` dispara **SÓ** para os frios (inverso do bloqueio); `buildPayload` branча recycle vs cold_block e força `keepTemplates=true` (reciclagem nunca apaga template). Cron/token/tracking/sent 100% reusados. CRUD via `project_schedules` (DTO ganhou `recycle*`; `list(orgId, projectId, recycle=false)` filtra; controller aceita `?recycle=true`). `project-schedules-sent` list aceita `?recycle=true|false`. **Separação:** página normal de Schedules/Sents passa `recycle=false`; aba **Leads → Automações → Reciclagem** (`RecycleTab.jsx`, espelho da página de Schedules + seletor "quem é frio" + contagem ao vivo + templates + envios) passa `recycle=true`. Endpoint one-shot `POST /email/projects/:id/recycle` existe mas a UI é toda schedule-based.
3. **Entrega/bounce (webhooks Resend) + supressão automática (FEITA):**
   - **Correlação:** o micro (`email-micro-services/service.js`) marca cada e-mail com `tags:[{name:'dispatch_id', value:<sentRow.id>}]` — os webhooks devolvem essa tag, atribuindo o evento ao disparo.
   - **Endpoint:** `POST /webhooks/resend` (público, SEM JWT) em `back/src/webhooks/resend-webhooks.{controller,service}.ts`. Verifica a **assinatura Svix** (HMAC-SHA256 sobre `${svix-id}.${svix-timestamp}.${rawBody}`, chave = base64decode do segredo sem `whsec_`; tolerância 5min; timingSafeEqual). **rawBody** capturado via `verify` hook no `json()` do `main.ts` (`req.rawBody`). **Idempotência** por `svix-id` (tabela `email_webhook_events`, PK = svix-id; P2002 ⇒ ignora reentrega). Segredo em `system_settings.resend_webhook_secret` (Integrações). Sem segredo ⇒ 200 `ok:false` (não processa); assinatura errada ⇒ 401.
   - **Eventos:** `email.delivered`→`delivered_count++`; `email.bounced`→`bounced_count++` e, se hard (default; só ignora Transient/soft), **suprime**; `email.complained`→`complained_count++` e suprime. Counts novos em `email_projects_schedules_sent` (delivered/bounced/complained_count; incremento por dispatch_id).
   - **Supressão:** `email_leads.global_status` = `BOUNCED`|`COMPLAINED` (enum já existia). Global por endereço (conta Resend é única na plataforma). O disparo já filtra `global_status:ACTIVE` ⇒ lead sai de TODOS os próximos envios automaticamente. `project-leads` expõe `global_status`/`suppressed`; Leads.jsx mostra chip "Suprimido · bounce/spam"; Sents.jsx e a aba Reciclagem mostram chips de entregues/bounces/spam por disparo.
   - **Config do usuário:** Integrações → cadastra a URL `https://api.bluewebchat.online/webhooks/resend` no Resend (eventos delivered/bounced/complained) e cola o Signing Secret.
4. **Automações por comportamento (gatilhos open/click) + analytics (FEITA):**
   - **Gatilhos:** tabelas `email_behavior_triggers` (event OPEN|CLICK, action ADD_TAG|SEND_TEMPLATE, tags/template_id, active) + `email_behavior_trigger_fires` (PK trigger_id+lead_id = **dispara-uma-vez por lead**) — migration `..._add_behavior_triggers` (FK→email_projects ON DELETE CASCADE no banco; sem relação no schema.prisma). Módulo `email-marketing/triggers` (CRUD `/email/projects/:id/triggers` + `TriggersService.fire(projectId, email, event)` **best-effort, nunca lança**). `fire()` é chamado dentro de `WebhooksService.registerOpenPixel`/`registerCtaClick` (WebhooksModule importa TriggersModule). ADD_TAG faz append em `email_leads.tags`; SEND_TEMPLATE envia 1 template pra 1 lead (reusa micro /send, cobra token, grava sent). Front: aba **Gatilhos** em Automações (`TriggersTab.jsx`).
     - **Frequência (FEITA):** cada gatilho tem `frequency` JSON `{ type:'once'|'unlimited'|'cooldown', value?, unit?:'minutes'|'hours'|'days' }` (migration `..._add_trigger_frequency`). `email_behavior_trigger_fires` ganhou `fired_count`+`last_fired_at`. `fire()` checa o registro (trigger,lead): once=1x; cooldown=respeita janela via `last_fired_at`; unlimited=sempre. Upsert incrementa contador. UI: seletor "Com que frequência (por lead)" no dialog + chip no card.
   - **Analytics:** `GET /email/analytics/overview?project_id?&from?&to?` (`email-marketing/analytics`) agrega `email_projects_schedules_sent` (disparos, leads, entregues, aberturas, cliques, bounces, spam + taxas), série temporal por dia, engajamento atual dos leads (clicked/opened/none/suprimidos via pivô metrics + global_status), e breakdown por projeto. Front: página **Analytics** (`views/pages/email/Analytics.jsx`, apexcharts — área temporal + donut de engajamento + KPIs + tabela por projeto), item de sidebar em Email Marketing (`/email/analytics`).
   - **Gotcha de teste:** `email_project_leads` criado via INSERT mysql cru falha silenciosamente (strict/JSON default) ⇒ o vínculo lead-projeto não existe e o gatilho não acha o lead. Testar sempre com dados criados via Prisma (fluxo real). Comprovado: com dados Prisma, `fire()` acha o link, aplica a tag e não duplica no 2º evento.

### Melhorias de e-mail — deliverability & construtor (jul/2026, mais recente)
Sequência de melhorias entregues uma por deploy (workflow do usuário: implemento → deploy → explico como testar → ele valida → OK → próxima):
- **Preheader** por template/e-mail (texto de prévia na inbox) — `model.preheader` → `<div>` oculto no topo. Ver seção do builder.
- **Cliques por-lead** — tabela `email_schedules_sent_clicks` + `click_unique_count` (ver Segmentação). Destrava condições de clique e CTOR real.
- **Reenvio p/ não-abridores** — `resendToUnopeners` (ver gotcha #8).
- **Teste A/B** de assunto+template (ver seção Teste A/B).
- **From Name por template** (fallback pro do projeto) — em TODOS os construtores + toda cadeia de envio + coluna `email_projects_schedules_sent.from_name`. Ver seção do builder.
- **Blocos novos no construtor:** card (ícone+título+descrição), colunas (2 células img/texto), barra colorida no topo, botão largura total — HTML simples mantido. Ver seção do builder.
- **Imagem rápida no e-mail:** sharp (resize/compress/JPEG) + nginx serve `/uploads` direto + cache 1 ano. Ver a seção da Imagem no builder e gotcha #5.
- **Prompt do Claude** (importação) reforçado com regras anti-Promoções + coluna `from_name`.

### Validação de e-mail + higiene (jul/2026) — bug "0/46 não enviou"
**Causa raiz:** um lead com e-mail malformado (`bonisiwed29@gmail..com`, ponto duplo) fazia a **Batch API da Resend rejeitar o LOTE INTEIRO** (`Invalid 'to' field`) → 0 enviados. A regex frouxa `\S+@\S+\.\S+`/`[^\s@]+@[^\s@]+\.[^\s@]+` usada em TODAS as portas de entrada **aceitava ponto duplo**. E o micro **engolia** o erro (só `console.error`) → o back marcava "Concluído" com `sent_for_leads=0`, sem `error_message`, e **cobrava token**.
- **Validação canônica:** `back/src/common/email.util.ts` (`normalizeEmail`, `sanitizeEmail`, `isValidEmail` ESTRITO — rejeita `..`, ponto na ponta, etc., `cleanEmailOrNull`, `classifyEmail`). `sanitizeEmail` CONSERTA erros comuns (ponto duplo, vírgula→ponto, `<...>`, `mailto:`, espaços). Aplicado em TODAS as entradas: quiz (`quizzes.service` `normalizeEmail`), webchat (`webchat.service` `normalizeEmail`), form/API (`leads.service` `publicSubscribe`+`create`), importação (`import.service` `normalizeRow`). **E-mail inválido/insanável não entra mais** (vira null/'' → não roteia; no form/API dá `BadRequestException`).
- **Micro resiliente** (`email-micro-services/service.js`): **pré-filtra** e-mails inválidos antes do lote (validador espelhado); se `resend.batch.send` falhar, faz **fallback envio individual** (um `to` ruim não derruba os bons); reporta `invalid_count`/`failed_count`/`error_message` no callback.
- **Back propaga + estorna:** callback `POST /webhooks/email/schedule-sent/:id` (`webhooks.service.updateScheduleSent`) grava `error_message` e, se terminou com **0 enviados**, **estorna os tokens** (`refundDispatchTokens`, idempotente, `type:'TOKEN_REFUND'`, zera `tokens_cost`). DTO `UpdateScheduleSentDto` ganhou `invalid_count`/`failed_count`.
- **Cron de higiene (rede de segurança, 1x/hora):** `email-marketing/hygiene/email-hygiene.service.ts` (`EmailHygieneRunner`, `@Cron(EVERY_HOUR)`). Pré-filtro SQL pega e-mails suspeitos, `classifyEmail` decide: **conserta** os consertáveis (com checagem de colisão → se o consertado já existe, remove o dup), **remove** os insanáveis (`email_leads.delete`, cascade limpa `email_project_leads`). E-mails válidos nunca são tocados. **Sem migration.**
- **Verificações de infra (jul/2026):** a conta Resend é **única na plataforma** (`system_settings.resend_api_key`, lida via `getResendApiKeyOrFail`). API key válida testável em `GET api.resend.com/domains`; webhooks da conta em `GET api.resend.com/webhooks`. Domínio verificado atual: `mail.glimuta.shop`. O `from` do projeto (`settings.sender.fromEmail`) precisa usar um domínio verificado nessa conta.

**Próximo item pendente (não iniciado):** _Alertas + auto-pausa de segurança_ (saldo baixo, taxa de spam/bounce alta → avisar e/ou pausar disparos automaticamente).

### Email — Formulário de captação v2: código único (WP) + config por projeto (set/2026)
Substituiu o par "Código 1: DIV" + "Código 2: SCRIPT" por **UM único snippet** estilo WordPress: `<!-- wp:html -->` (`<style>` + `<script>`) + `<!-- wp:group className:"pmf-form-wrap" -->` com os campos num `wp:html`, texto de destaque em `wp:paragraph`, **botão real `wp:button` (com `href`)** e nota em `wp:paragraph`. Colável no **editor de código** do WP onde o formulário deve aparecer — e depois **editável no editor visual** (destaque/botão/nota são blocos core). Também funciona em qualquer HTML (os comentários `wp:*` são ignorados; o CSS estiliza as classes `.wp-block-*`).
- **Arquivos:** `front/.../email/captureFormSnippet.js` = gerador **PURO** (sem React; testável em Node): `defaultCaptureConfig`, `normalizeCaptureConfig(raw)` (nunca lança; mescla JSON antigo/parcial com defaults), `buildCaptureSnippet(cfg, {apiBase, organizationId, projectId})`, `buildPreviewDoc(snippet)`, escapes `escText/escAttr/safeJson` (`safeJson` neutraliza `</script`, `<!--`, U+2028/9). `WebhookLeadsForm.jsx` = modal (abas **Configuração / Preview / Usar API**; API inalterada). `EmailMarketing.jsx` passa `onSaved={() => mutate()}`.
- **Config persistida por projeto** em **`email_projects.settings.capture_form`** (JSON livre, sem migration). Save = `GET /email/projects/:id` (fresco) → `{...settings, capture_form}` → `PATCH /email/projects/:id {settings}`. **O PATCH substitui `settings` inteiro** — NUNCA mandar só `capture_form` (apagaria `sender`/`cold_block`/`flow_interacted_only`). "Copiar código" **salva automaticamente** se houver alteração (snackbar avisa). Botões "Salvar configuração" / "Restaurar padrão" no rodapé; chip "Alterações não salvas".
- **Config (tudo personalizável):** campos nome (toggle, default ON) / e-mail (sempre) / **telefone (toggle, default OFF)** com rótulo+placeholder; botão (texto, **URL de destino**, cor fundo/texto/brilho pulsante); **texto de destaque acima do botão** (laranja; vazio = não aparece) + cor accent (também é o foco dos inputs); **texto abaixo do botão** (nota com cadeado; default "Você permanecerá no mesmo site"; vazio = não aparece) + cor; **feedback visual (toggle, default OFF)** + mensagem de sucesso (default vazia) / erro; largura máx (px, default 412); evento do Facebook Pixel no clique (default `CliqueBotao`, vazio = não dispara); attributes extras JSON.
- **Script do site** (ES5, listeners delegados no `document`, config em `window.__PMF__[formId]` + guarda `window.__PMF_BOUND__` → vários formulários/colagens na mesma página ok): clique no botão **SEM e-mail → segue o link normalmente** (sem API); **COM e-mail → `preventDefault` + POST `/email/leads/subscribe/:org/:project`** com `{email, attributes:{...extras, phone?}}` e `name` **só se preenchido** (o back já aceitava `name` opcional — sem mudança no servidor), `fetch keepalive` + **timeout 4s** (nunca prende o lead: API lenta/erro → redireciona mesmo assim) → redireciona pra URL do botão (sem URL = só cadastra). Feedback só se `feedback.enabled` (mostra ~1,2s e redireciona). Sem config (bloco do script apagado) → link normal. Enter no campo = clique. `formId = pmf-<8 chars do projectId>`; `data-pmf` fica no `<div class="pmf-form">` (dentro do `wp:html`).
- **Preview real:** `<iframe sandbox="allow-scripts" srcDoc>` roda o **MESMO snippet** com `window.__PMF_PREVIEW__=true` → clicar simula (mostra o que faria: salvar + redirecionar / ir direto) sem chamar a API nem navegar.
- **Gotcha WP (validação de bloco):** nos blocos core, o JSON do comentário e o HTML gerado precisam bater **1:1** (cores normalizadas pra hex minúsculo de 6 dígitos; `has-text-color`/`has-background`/`has-custom-font-size` presentes; `contentSize` do group = `--cta-max-width`). **Nunca** pôr `data-*` em wrapper de bloco core (o editor marca "conteúdo inválido"). Texto vazio ⇒ bloco omitido inteiro. `href` só com URL válida (normaliza `https://`, `&` → `&amp;`).
- **Testes (scratchpad, Node — rodar antes de mexer no gerador):** `test_snippet.mjs` (escape de `</script`/`<!--`, JSON↔inline por bloco, comentários `wp:*` balanceados, `new Function(script)`, clamp/normalizações, blocos omitidos) e `test_runtime.mjs` (jsdom instalado só no scratchpad: sem/com e-mail, payload exato, name omitido, phone em attributes, feedback on/off, erro, sem script, Enter, preview, 2 formulários). Build local do front (`npx vite build --outDir <scratch>`) também roda antes do deploy.
- **Gotcha de tooling (IMPORTANTE):** escapes `\uXXXX` "nus" em parâmetros de tool viram o **caractere literal** no arquivo (ex.: U+2028 dentro de um regex literal = `SyntaxError: missing /`). Outros backslashes chegam intactos. Pra caracteres especiais em código, usar `String.fromCharCode(0x2028)`; nunca escrever o escape nu. Sempre validar com teste em Node após escrever arquivos com regex/escapes.

### Front-end — profissionalização (Fase 1, ago/2026)
Reforma visual do painel (template Berry MUI v7). Trabalho por fases (usuário manda prints/itens → checklist → desenvolve → deploy → valida). **Fase 1 (FEITA):**
- **Tema dark completo** — `themes/index.jsx` registra `colorSchemes.light` **e** `dark`; paleta em `themes/palette.jsx` (`buildPalette` devolve `{light, dark}`), tokens dark em `themes/theme/default.js` (slate-navy: `darkBackground #0b1120`, `darkPaper #151d30`, grey **invertido** p/ dark, tints semânticos). Light ganhou profundidade (`background.default = grey50`, paper branco). `custom-shadows` usa base preta no dark. Toggle sol/lua no **rodapé da sidebar** (`useColorScheme().setMode`), persistido (`modeStorageKey='theme-mode'`). Default = light (`config.js DEFAULT_THEME_MODE`).
  - **GOTCHA CRÍTICO do dark:** com CSS variables, `theme.palette.X` (em callbacks sx/styled) trava no valor do **light** e NÃO troca no dark. Use **`theme.vars.palette.X`** (ou token string `'background.paper'`, que troca sozinho). Para tints com alpha: **`` `rgba(${theme.vars.palette.X.mainChannel} / 0.12)` ``** (não `alpha(theme.palette…)`). Para estilo só-no-dark: `theme.applyStyles('dark', {...})`. Corrigido nas telas (EmailMarketing, Transactions, Webchats, SegmentBuilder, AbTests, Avatars, Posts, Analytics charts, Dashboard). **Renderizadores públicos (PublicWebchat/Quiz, QuizView, builders de webchat/quiz/e-mail preview) NÃO seguem o tema do app** — usam a cor que o cliente configura; não tokenizar.
- **Fonte Ubuntu** — `config.js fontFamily` + `<link>` Google Fonts no `index.html`. **GOTCHA:** o config é semeado no localStorage (`useLocalStorage`) — mudar `config.js` não afeta quem já tem config salvo. Solução: **versionar a chave** em `contexts/ConfigContext.jsx` (`'berry-config-vite-js'` → `'pubmail-config-v2'`) pra forçar re-seed. Tipografia (`themes/typography.jsx`): 700 bold / 500 medium / 400 corpo; botões `textTransform:none` peso 600.
- **Logo** — `ui-component/Logo.jsx` virou **wordmark inline** "Pub Mail" (SVG/CSS, theme-aware; "Mail" no `primary.main`); `variant="mark"` = selo "PM". Usada em Login/Register/Home e no topo da sidebar. **Favicon** = `public/favicon.svg` (monograma "PM" azul); `index.html` limpo (removido o **pixel de tracking do codedthemes**, lang pt-BR, theme-color).
- **Login** enxuto (`Login.jsx`): só logo + campos + Entrar (sem boas-vindas, sem link de cadastro; rota `/register` existe mas não é linkada).
- **Header removido** — `MainLayout` não tem mais AppBar; logout foi pro **rodapé da sidebar** (`Sidebar/SidebarActions.jsx`: perfil com inicial+nome+email, toggle de tema, sair). Gatilho de menu só no mobile (`MainLayout` IconButton fixo). `MainContentStyled` marginTop 88→24.
- **Sidebar** (`Sidebar/index.jsx` + `SidebarToggle.jsx` + `SidebarActions.jsx`): grupos viram **accordions** (`MenuList/NavGroup` — abertos por padrão, persistidos em `pm-sidebar-group-<id>`), logo no topo ao lado do colapsar, ícone dedicado de recolher/expandir. Removidos do shell: `Header/*`, `Customization`, `MenuCard`, `UpgradePlanCard`, `SearchSection`, `NotificationSection` (dead code do Berry).
- **Dashboard** (`views/dashboard/Default/index.jsx`): cards de resumo (Projetos+leads, Agentes, Webchats, Quizzes, Integrações de IA). Back novo: **`GET /dashboard/overview`** (`back/src/dashboard/*`, org-scoped: counts + `ai-keys-status` reusado).
- **Polish de cards** (`MainCard.jsx`): borda sutil + sombra suave por padrão (theme-aware via `applyStyles('dark')`).
- **Inputs** (`overrides/OutlinedInput.jsx`): neutraliza o **autofill azul do Chrome** (`-webkit-autofill` → box-shadow inset com `background.paper`), borda `divider` + foco `primary`. Resolve o "campo azul / olho descolado" do login.
- **GOTCHA Grid v7:** MUI v7 **ignora** a API antiga `<Grid item xs md>` → layout quebra (KPIs 7+1). Usar **`<Grid size={{ xs, md }}>`**. Corrigido em Analytics e AbTests; **ainda pendente:** Catalog, Posts (periféricos).
- **Fase 2/3:** aguardando orientações do usuário (mais ajustes de tela por prints).

### Integrações — chaves de IA mascaradas + add/remove por chave (ago/2026)
Redesenho da tela **Conta & Domínios → Integrações** (`IntegrationsManager.jsx`). Antes: um `<textarea>` CSV por provedor que voltava **vazio** (o back mandava a chave crua e o Save podia apagar tudo). Agora, profissional + **seguro** (a chave crua NUNCA vai pro browser):
- **Back (`system-settings.service.ts`):** const `AI_COLUMNS` (provider→coluna); `maskKey` (`gsk_...XF7f`), `maskedKeys`, **`addAiKeys(provider, keysCsv)`** (merge+dedupe via Set, aceita CSV/linha, `BadRequestException` p/ provider inválido/vazio), **`removeAiKey(provider, index)`** (splice por índice, valida range).
- **Back (`system-settings.controller.ts`):** `toResponse` agora devolve **`<provider>_api_keys_masked: string[]`** + `_count` (SEM chave crua — só resend/webhook/edge/certbot voltam em claro pois são de campo único editável). Endpoints novos **`POST /system-settings/ai-keys {provider, keys}`** e **`DELETE /system-settings/ai-keys {provider, index}`** (ambos retornam o `toResponse`).
- **Front (`IntegrationsManager.jsx`):** por provedor, **chips mascarados** (`IconKey`) com `onDelete` (× → `remove('/system-settings/ai-keys', { data:{provider,index} })`) + campo "Adicionar chave(s)" (vírgula separa várias, Enter envia) → `post('/system-settings/ai-keys', {provider,keys})`; recarrega settings a cada op (`applyServerData`). **As chaves de IA saíram do Save geral** (`handleSaveInfra` só PATCHa resend/webhook/edge/certbot) → Save nunca mais apaga chave. Card do provedor fica com borda/tint `success` quando tem ≥1 chave. Chip-total "N chaves ativas" no topo.
- **Gotcha:** `remove(url, config)` do api client é `axios.delete(url, config)` → body via `{ data:{...} }`. Um DTO órfão `dto/update-ai-keys.dto.ts` (de tentativa anterior, importava `PROVEDORES_IA` inexistente) ficou só no server e quebrou o `nest build` — removido com `rm -f` no deploy.

### Módulo Telegram (EM CONSTRUÇÃO — começando do zero, ago/2026)
Novo módulo grande, sendo especificado **aos poucos** pelo usuário (ele orienta parte por parte; anotar TUDO aqui à medida que evolui). **Liberdade total de layout/UX/UI** — nada de legado pra respeitar, então caprichar no visual ("cada pixel importa").
- **Escopo pretendido (visão geral, ainda abrindo):** mensagens em massa / **broadcast**, **grupos**, **chat**, **bot**, **IA**, e **pagamento** (billing/cobrança dentro do fluxo Telegram). Detalhes de cada frente virão do usuário.
- **Parte 3 — Fluxo Inicial (EM CONSTRUÇÃO, por fases; decisões: reactflow visual, 1 fluxo por bot, áudio gravado→voice via ffmpeg):**
  - **Fase 1 (FEITA):** sidebar item **"Fluxo Inicial"** (`/telegram/flows`, `IconSitemap`). Tabela **`telegram_flows`** (migration `20260822130000_add_telegram_flows`; 1 por bot via `@@unique(bot_id)`, `active`, `definition` JSON = grafo reactflow, `start_node_id`, FK→telegram_bots CASCADE). Back `TelegramFlowsService`/`Controller`: `GET/PUT /telegram/bots/:id/flow`. Front `views/pages/telegram/TelegramFlows.jsx` = **construtor visual reactflow** (reusa `reactflow@11` já instalado): seletor de bot + toggle ativo + "Salvar"; nós `start`+`message`; MessageNode com texto, **botões** (ação: próximo passo / abrir link / entrar no grupo / ir para outro bot — handles dinâmicos via `useUpdateNodeInternals`) e **respostas por texto** (keyword→handle) + fallback "qualquer resposta"; painel editor lateral; **LinkPicker** (grupo com @username→t.me / outro bot / URL). `definition={nodes,edges}` salvo como JSON. **Mídia/áudio e runtime ainda NÃO** (fases seguintes).
  - **Fase 2 — runtime no webhook (FEITA):** tabela **`telegram_flow_enrollments`** (migration `20260822160000_add_telegram_flow_enrollments`; `@@unique(bot_id, tg_user_id)`, `current_node_id`, `status` ACTIVE/COMPLETED/STOPPED, `context` JSON, FK bot+contact CASCADE). `TelegramFlowRuntimeService.handleUpdate(bot, update)` é chamado no webhook **depois** do `captureUpdate`: **/start** → reseta enrollment + envia o nó destino do handle `out` do start; **botão** (`callback_query` data `fb:<buttonId>`) → segue a aresta `btn-<buttonId>`; **texto** → casa `answers` (keyword `includes`) via `ans-<id>`, senão `fallback`. `advance()` envia o nó + grava `current_node_id`, marca COMPLETED se o nó não tem aresta de saída. Interpola `{{nome}}/{{name}}`, `{{sobrenome}}`, `{{username}}`. Botões viram `inline_keyboard` (action next=callback, url/group/bot=url). Manda `sendChatAction('typing')` antes. **As mensagens OUT do bot são logadas em `telegram_messages` (contact)** → aparecem nas DMs. Util ganhou `sendMessage(...,extra)`, `answerCallbackQuery`, `sendChatAction`. **Sem delays temporizados ainda** (envio imediato passo-a-passo conforme o usuário responde). Fase 5 (FEITA — ver abaixo).
  - **Melhorias pós-Fase 5 (ago/2026):** (1) **Múltiplas mensagens por nó** do fluxo: `data.messages=[{id,text,media,delay_seconds}]` (compat: nó antigo `{text,media}` vira lista de 1); runtime `sendNode` envia a sequência, **botões só na última**, `delay_seconds` por mensagem (typing). `NodeEditor` reformulado (lista de Mensagens usando `TelegramMediaField` compartilhado, seção Botões "na última", Segmentação no fim). (2) **LinkPicker de grupo** lista **todos os grupos do bot** (não só públicos); grupo privado → gera **link de convite** via `GET /telegram/groups/:id/invite-link` (`createChatInviteLink`, bot precisa ser admin c/ permissão de convidar). (3) **Automações em fluxograma vertical por tempo**: nó "Lead dá /start" + conectores com o atraso (delay editável no conector) + cards; preview de mídia no passo. **Gotcha comum:** fluxo é **por bot** — se cadastrou no bot errado, dá pra copiar o `definition` entre `telegram_flows` no banco (feito 1x: blue_grid→mia). (4) **Zoom do reactflow**: `fitViewOptions={{maxZoom:0.85, padding:0.35}}` + `minZoom={0.2}` (sem isso o `fitView` amplia demais com poucos nós). (5) **Automações — lista com balõezinhos**: cada card mostra mini-timeline "Lead dá /start → após X min → …" com prévia de cada passo (`stepPreview`). (6) **Grupo duplicado** (migração grupo→supergrupo gera 2 linhas com `tg_chat_id` diferente): dedupe no front do `TelegramLinkPicker` (mantém o com `member_count`) + no `captureUpdate` trata `migrate_to_chat_id`/`migrate_from_chat_id` (apaga a linha antiga).

### Módulo Telegram — polimentos (ago/2026)
- **Viewport do fluxo no topo-esquerda:** `TelegramFlows` usa `useReactFlow().setViewport` (helper `alignNodes` = alinha o menor x/y dos nós a ~(40,40), zoom 0.8) chamado após load/buildDefault; `defaultViewport={{x:40,y:40,zoom:0.8}}` (removido o `fitView`, que centralizava).
- **`SearchSelect.jsx`** (Autocomplete com busca) — usado em TODO seletor de bot/grupo grande: `TelegramFlows` (bot), `TelegramDMs` (bot + "Todos"), `TelegramGroups` (filtro bot), `TelegramLinkPicker` (grupo/bot/deep-link). `options=[{value,label}]`.
- **Balões da lista de automações** (`TelegramAutomations`): mini-timeline com bolhas estilo chat (borda/sombra, thumbnail da foto quando `media.type==='photo'`, emoji p/ outras mídias).
- **Dedupe de grupo morto (migração):** `listGroups` agrupa por `bot_id+título`; se há uma viva (`member_count!=null`) e uma morta (null), **apaga a morta** (`deleteMany`) — corrige Grupos E DMs, roda no próprio GET. Combinado com o handler de `migrate_to_chat_id` no `captureUpdate` (previne). Grupo supergrupo tem `tg_chat_id` com prefixo `-100`.

### Módulo Telegram — Broadcasts (FEITA, ago/2026)
Disparo em massa (DMs + grupos + canais) de vários bots, com **rotação de copies**, **agendamento diário** e **fila de alta performance**. Decisões: horários **recorrentes todo dia**, **1 copy por disparo** (rotação/aleatório), **sem dedupe** nas DMs (envia por cada bot).
- **Modelo (migration `20260824150000_add_telegram_broadcasts`):** `telegram_broadcasts` (`bot_ids[]`, `targets{dms,group_ids[],channel_ids[]}`, `times[]`=minuto-do-dia BRT, `mode` sequential|random, `delete_used`, `active`, `last_copy_index`), `telegram_broadcast_copies` (`position`, `messages[]`=texto/mídia/`delay_seconds`), `telegram_broadcast_runs` (1 por disparo, `@@unique(broadcast_id, run_at)`, snapshot `messages`, métricas total/sent/failed/blocked), `telegram_broadcast_sends` (fila: `status` PENDING/DONE/FAILED/BLOCKED, `target_kind`, `tg_chat_id`, `attempts`).
- **Engine (`telegram-broadcasts.runner.ts`):** **dispatcher `@Cron(EVERY_MINUTE)`** → se o minuto-do-dia (BRT via `Intl`) bate com `times[]` do broadcast ativo e não há run (idempotência) → cria run, **escolhe a copy** (sequencial via `last_copy_index` / aleatório), snapshota as mensagens, **resolve audiência** (todos os `telegram_contacts` dos bots + grupos/canais escolhidos) e `createMany` os sends (PENDING em lotes de 1000); se `delete_used`, apaga a copy. **Worker contínuo** (`onModuleInit` → loop): puxa lote de PENDING (200), pool de concorrência 40, **token-bucket por bot (~25 msg/s, `botNextAt`)**, `429`→`botCooldown` respeita `retry_after` (send fica PENDING), `403`→BLOCKED, erro→retry até `MAX_ATTEMPTS=5`→FAILED; envia cada mensagem da copy com o `delay_seconds` (typing) e **loga OUT** em `telegram_messages` (aparece nas DMs); reconcilia métricas da run por `groupBy`. **"Disparar agora"** = `fireNow` (run_at com segundo atual, não colide com a agendada).
- **Back (`telegram-broadcasts.service`/`controller`):** CRUD `/telegram/broadcasts` (+ `sanitizeInputs` valida ids contra a org, `times` 0..1439 dedupe/sort), `PUT/GET :id/copies` (substitui a lista), `POST audience-estimate` (conta DMs+grupos+canais), `GET :id/runs` (histórico), `POST :id/fire`.
- **Front (`views/pages/telegram/`):** `TelegramBroadcasts.jsx` (rota `/telegram/broadcasts`, substituiu placeholder) — lista de cards (alvos/horários/modo/copies/ativo + ações Copies/Editar/Disparar/Histórico/Excluir); **modal de broadcast** (multi-select de bots com busca via `Autocomplete`, toggle DMs + multi-select grupos/canais, lista de horários `type=time`→minuto-do-dia com chips, modo sequência/aleatório + "apagar copy usada", **estimativa de audiência ao vivo**); **modal de copies** (lista de copies, cada uma = `MessagesEditor` [texto+`TelegramMediaField`+atraso] + **`TelegramMessagesPreview`** ao lado); **modal de histórico** (runs com métricas). `TelegramMessagesPreview.jsx` = balões estilo Telegram (dark/light).
  - **UI da listagem (FEITA, ago/2026):** cards viraram **linhas full-width** (estilo tabela): badge gradiente Telegram + nome + chip Ativo/Inativo, linha de **audiência** (`dms_count` vem do back — `list()` faz `groupBy` de `telegram_contacts` por bot e soma os bots do broadcast quando `targets.dms`), chips de modo/copies/horários, e ações compactas (Disparar contained + ícones Copies/Editar/Histórico/Excluir com tooltip; excluir tem `window.confirm`). **Toolbar de filtros** (client-side): busca por nome, multi-select de bots, multi-select de grupos, select de horário (distintos) + "Limpar". **Copies viraram `Accordion`** (colapsáveis; header mostra nº de mensagens + prévia do 1º texto + remover; **fechadas por padrão** — só a copy recém-adicionada abre, via `openOnMount`).
  - **Status por horário + histórico rico (FEITA, ago/2026):** cada chip de horário (`TimeChip`) mostra um **badge de status de HOJE** no canto: ✅ verde (enviado ok), ⚠ âmbar (enviado com falhas), ❌ vermelho (executou e falhou), 🕐 amarelo (aguardando), spinner azul (enviando), relógio cinza (não rodou hoje). O back (`list()`) calcula `time_status: {minuto→estado}` cruzando os **runs de hoje (BRT)** com os `times[]`: helpers `brtNowParts()`/`brtMinuteOfDay(date)`, `startOfToday = now - (h*3600+m*60+s)*1000`, casa run↔horário por `brtMinuteOfDay(run.run_at)===t` (mais recente vence); sem run → `pending` (t≥agora) ou `missed`. **Histórico** (`RunsModal`) reformulado: `GET :id/runs?page&page_size&status&from&to` (service `listRuns` paginado — `status` all/clean/errors/sending/done, período via from/to, retorna `{items,total,page,pages}`); front com **filtros** (status + período Hoje/7d/30d) + **paginação** + cards com ícone de resultado (`runOutcome`), **barra de proporção** enviados/bloqueados/falhas (`RunBar`, cores por token string — `bgcolor:'success.main'`, NÃO função), pílulas de métrica (`StatPill`) e % entregue.
  - **Variáveis + botões nas copies (FEITA, ago/2026):** cada mensagem da copy agora tem **interpolação de variáveis** e **botões inline (link)**. **Worker** (`sendOne`) interpola `{{nome}}/{{sobrenome}}/{{username}}/{{origem}}` por destinatário — `processBatch` monta um `contactMap` (id→first_name/last_name/username/attributes.start_param) e passa ao `sendOne`; em grupos/canais (sem contato) as variáveis viram vazio. **Botões:** shape `message.buttons=[{id,label,url}]` (só link, dentro do JSON da mensagem — sem migration; snapshot na run cobre automaticamente). Service `sanitizeButtons`+`normalizeButtonUrl` (máx 6/msg, prepend `https://` em `t.me/…`/domínio/@user, descarta inválidos — link ruim faria o `sendMessage` inteiro falhar). `sendOne` monta `reply_markup.inline_keyboard` (1 botão/linha). **Front `MessagesEditor`:** chips clicáveis de variável que **inserem no cursor** do textarea (via `inputRef` + `selectionStart`), editor de botões (label+URL) com **`TelegramLinkPicker`** embutido (ícone 🔗 → escolhe grupo/canal/bot/deep-link/URL, normaliza p/ `t.me`), preview renderiza os botões estilo Telegram. `CopiesModal` recebe `bots` e deriva `botId` (1º bot do broadcast) p/ o picker listar grupos/canais.

### Módulo Telegram — Redesign nível Typebot + Design-System (FEITA, ago/2026)
Reforma visual/UX de TODO o módulo Telegram inspirada no Typebot, feita em paralelo (5 agentes) sobre uma **biblioteca de design-system** nova: `front/.../telegram/ui/index.jsx` — átomos reusados por todas as páginas, **dark-mode correto via `theme.vars`** (NUNCA `theme.palette` em callback sx, que congela no light): `TgPageHeader`, `TgEmptyState`, `TgCard` (accent+hover), `TgStatusPill` (bolinha colorida), `TgAvatar` (envolve `AuthImage` + fallback gradiente por kind bot/group/channel/contact), `TgFilterBar` (boxed), `TgModal` (header+footer **sticky** + X), `TgConfirmDialog` (mata `window.confirm`), `TgGhostButton`, `TgCardGridSkeleton`/`TgListSkeleton`. Componentes específicos do fluxo em `telegram/ui/flow.jsx`. **Gotcha do tint:** `` `rgba(${theme.vars.palette.X.mainChannel} / a)` `` só p/ cores semânticas com `.main` (primary/success/error/warning/info); p/ neutro use tokens `'action.hover'`/`'action.selected'` (grey/text NÃO têm `mainChannel` confiável).
- **Páginas migradas:** **Grupos/Canais** (`TelegramGroups` genérico — cards TgCard, avatar TgAvatar, filtros boxed, empty rico, sair com `TgConfirmDialog`); **Configurações** (`TelegramSettings` — bot cards TgCard, token em code-row copiável, Revalidar/Editar/Excluir em menu `⋮`, status TgStatusPill, add/edit em TgModal); **`BotProfileDialog`** (TgModal, seções Foto/Perfil/Comandos/Status, preview via `applyStyles`); **Broadcasts** (rows TgCard, **`TgConfirmDialog` no "Disparar"** com resumo de audiência, TimeChip com bolinha à esquerda + tooltip, 3 modais → TgModal sticky, toolbar responsiva com Popover "Filtros" no mobile, ações secundárias em `⋮`); **DMs premium** (bolhas sólidas sem borda com cauda + agrupamento + separadores de data + wallpaper, composer com **FAB** circular, header sem cronômetro `sync` — vira `TgStatusPill online`, altura `100dvh-88px`, nav 2-níveis no mobile); **Automações** (preview `TelegramMessagesPreview` ao vivo ao lado do editor, cards TgCard, edit em TgModal, condição destacada, título derivado quando nome é default); **`TelegramMessagesPreview`** (cores Telegram fixas via helper `dual`/`applyStyles` — corrige o bug do `palette.mode` — + typing-dots animado); **`TelegramMediaField`** (afford compacta → drop-zone com abas + drag-drop de arquivo + gravação com play-antes-de-enviar + barra de progresso); **`TelegramLinkPicker`** (grade de cards tipados com ícone + copiar deep-link, TgModal); **`SearchSelect`** (opção rica opcional `{secondary,avatarPath,icon,status}` — retrocompatível).
- **Construtor de fluxo (flagship, `TelegramFlows` + `ui/flow.jsx`):** canvas full-width + **paleta esquerda de blocos ARRASTÁVEIS** (categorias Mensagens/Entradas/Lógica, cor+ícone) → HTML5 DnD (`dataTransfer('application/tg-block')`); `onDrop` usa `screenToFlowPosition` + `document.elementFromPoint(...).closest('.react-flow__node')[data-id]` p/ decidir **anexar ao nó** vs **criar nó novo** (seed do array certo). **Bloco "Atraso/Digitando…"** → mapeia `messages[].delay_seconds` (sem mudança no back), render como chip de reticências animado com slider 0–10s. Nós = balões Telegram reais (header inline-editável + menu `⋯` duplicar/excluir, porta por botão + fallback), seleção com glow; edges bezier de marca com ponto SMIL animado + delete-× na seleção; **minimapa dark corrigido** (era caixa branca sólida); `fitView({padding:0.3,duration:400})` no load (removido o hack `alignNodes`); `snapToGrid [16,16]`; inspector overlay que desliza na seleção; indicador Salvo✓/Salvando/Alterações-não-salvas. **Contrato do backend intacto** (`{nodes,edges}`, `node.data` `{title,messages[],buttons,answers,apply_tags}`, handles por `sourceHandle`); edges ganham `type:'brand'`+`markerEnd` no JSON (campos extras inofensivos, runtime ignora). Aba Automações segue renderizando `<TelegramAutomations/>`.

### Módulo Telegram — Mensagem rotativa / prova social (FEITA, ago/2026)
"Fulano acabou de aderir ao VIP" que fica **trocando no lugar** (não empilha): o bot posta UMA mensagem num grupo/canal e a **edita em loop**. Tabela **`telegram_rotators`** (migration `20260825140000_add_telegram_rotators`; `@@unique(group_id)` = 1 por chat, FK bot+group CASCADE; `messages` JSON, `name_pool` JSON, `interval_seconds` 5..3600, `mode` sequential|random, `current_index`, `current_message_id` BIGINT, `last_rotated_at`). **`TelegramRotatorsRunner`** (loop contínuo, tick 3s) → para cada rotativa ativa vencida edita via novo **`tg.editMessageText`**; se a msg foi apagada/inválida, **reenvia** e passa a editar a nova; erro "not modified" (aleatório repetiu texto) é ignorado. `{{nome}}` sorteia do `name_pool`. CRUD `/telegram/rotators` (`telegram-rotators.{service,controller,runner}.ts`; service valida bot/group da org, bloqueia 2 no mesmo chat). Front: sidebar **"Mensagem rotativa"** (`/telegram/rotators`, `IconRotateClockwise2`), página `TelegramRotators.jsx` (kit; textos um-por-linha + nomes p/ sortear + intervalo + modo + ativo). **Requer o bot admin do grupo/canal com permissão de postar.** Novos helpers no util: `editMessageText`, `deleteMessage`.
  - **Alvo "O próprio bot" / DMs (FEITA, ago/2026):** rotativa também nas **DMs** (uma msg por lead, editada em loop). Migration `20260825220000_rotator_dm_target`: `target_kind` ('group'|'dm'), `dm_cursor` INT, `group_id` **nullable** (dropou/readd a FK). Service: `target_kind='dm'` → não exige grupo, 1 rotativa de DM por bot. Runner: `dmSweep(r)` roda **todo tick** (não gated por interval) e varre os contatos do bot em **lotes de 20** (cursor `dm_cursor`, wrap no fim), editando/enviando a msg de cada um; texto atual = índice por tempo (`Math.floor(now/(interval*1000))%n`); msg id por contato em `telegram_contacts.attributes.rotator_msg_id`; `fill` usa o **nome real do lead** em `{{nome}}` (senão sorteia do pool); `sleep(120ms)` = ~8 msg/s por bot (rate-limit). 403 (bloqueou) é ignorado. **Atualização é gradual** em listas grandes (respeita limite do Telegram). **Mantém-se como ÚLTIMA mensagem:** `dmSweep` compara `attributes.rotator_msg_id` com o `MAX(tg_message_id)` de `telegram_messages` do contato — se afundou (chegou msg nova depois), **apaga e reenvia embaixo silencioso** (`disable_notification`); senão edita no lugar. Para ser **instantâneo**, o `flow-runtime.advance` chama `rotators.bumpForContact(botId, tgUserId)` após enviar cada nó (apaga+reenvia a rotativa embaixo na hora). Front: opção "🤖 O próprio bot (DMs de todos os leads)" no seletor (`DM_SENTINEL`), card mostra "DMs do bot". `{{nome}}` sorteia SEMPRE do `name_pool` (prova social = outros nomes, não o do lead).

### Módulo Telegram — Botão de plano (checkout) em copies/automações (FEITA, ago/2026)
O botão de **"Vender plano"** (checkout PIX) que existia só no construtor de fluxo agora também está nas **copies de broadcast** e nos **passos de automação**. Botão com `plan_id` (em vez de `url`) → o worker/runtime monta `callback_data:pay:<plan_id>` (broadcast `sendOne`, automação `flow-runtime.sendComposed`). Sanitizers aceitam `plan_id` (broadcast `sanitizeButtons`, automação `sanitizeSteps`). **Runtime `pay:` manda o PIX SEMPRE no privado do usuário** (`cq.from.id`, não o chat) — funciona quando o botão está num grupo/canal (toca no grupo → "te enviei no privado"). **Limpeza na confirmação (FEITA):** coluna `telegram_payments.nudge_message_id` (migration `20260825230000_payment_nudge_msg`) — `sendNotPaidNudge` apaga o aviso anterior e guarda o novo (só UM aviso por vez); no `deliver`, apaga o aviso obsoleto + `rotators.bumpForContact` empurra a rotativa pra baixo da confirmação. `startCharge` e `flow-runtime.advance` também chamam `bumpForContact` (rotativa sempre por último). Trava por contato (`busy` Set no runner) evita sweep+bump duplicarem a rotativa. Front: toggle **🔗 Link / 💎 Plano** por botão nos editores (`MessagesEditor` do Broadcasts e `StepCard` das Automações), com `SearchSelect` dos planos do bot (`GET /telegram/payments/plans?bot_id=`), espelhando o `ui/flow.jsx`.

### Módulo Telegram — Pagamentos / VIP (Fase B, FEITA, ago/2026)
Monetização: lead digita **`/vip`** (ou `/planos`/`/comprar`) no bot → botões de plano → toca → bot gera **PIX (copia-e-cola + QR)** + botão "✅ Já paguei" → validação **real** no gateway → ao confirmar, entrega **link de convite de uso único** do grupo/canal VIP + mensagem/mídia. Migration `20260825160000_add_telegram_payments`: `telegram_payment_settings` (por org, `@@unique(organization_id)`; `provider`, `access_token`, `pix_key`, `active`), `telegram_plans` (por bot, FK CASCADE; `name`, `description`, `price_cents`, `deliver_group_id` FK→telegram_groups SET NULL, `deliver_message`, `deliver_media` JSON, `active`, `position`), `telegram_payments` (cobranças; `provider_charge_id`, `amount_cents`, `status` PENDING/PAID/EXPIRED/FAILED, `pix_code`, `tg_message_id`+`tg_message_is_photo`, `invite_link`, `delivered`, FK bot CASCADE / plan+contact SET NULL).
- **Gateway CONFIGURÁVEL** (`telegram-payment-providers.ts`): abstração `createPixCharge`/`getChargeStatus`/`testProvider`/`extractWebhookChargeId` com 2 provedores — **Mercado Pago** (default; `/v1/payments` PIX, status via `/v1/payments/:id`, `approved`→PAID; teste via `/users/me`) e **PushinPay** (`/api/pix/cashIn` em centavos, status `/api/transactions/:id`). Fácil somar outros (switch por `provider`).
- **Runtime** (`telegram-payments.runtime.ts`, chamado no webhook depois do flow): comando `/vip|/planos|/comprar` → `sendPlanList`; callbacks `pay:<planId>`→`startCharge` (cria cobrança + envia PIX c/ QR via `sendPhoto`+caption HTML+botões, ou texto fallback), `paycheck:<paymentId>`→`checkPayment` (reconfere status; toast), `paycancel:`→`cancelPayment`. **Service** (`telegram-payments.service.ts`): CRUD settings (token nunca volta cru — `token_masked`)/plans/sales, `startCharge`, `checkPayment`→`markPaidAndDeliver`→`deliver` (cria invite `member_limit:1` expira 3d + msg/mídia + edita a cobrança removendo o botão; **idempotente**), `reconcilePending` (runner).
- **Webhook público** `POST /telegram/payments/webhook/:provider` (sem JWT, ACK 200) → extrai o charge id e **reconfere o status na API** antes de entregar (webhook forjado não fura). **Poller** `TelegramPaymentsRunner` (@Cron 1min) reconfere PENDING (rede de segurança) e expira as vencidas (30min).
- **Endpoints (JWT `/telegram/payments`):** `GET/PUT settings`, `POST settings/test`, `GET/POST plans` + `PUT/DELETE plans/:id`, `GET sales` (paginado, filtros status/bot + receita confirmada). Novos helpers util: `editMessageCaption`, `editMessageReplyMarkup`, `sendPhoto` c/ `extra` (parse_mode/reply_markup), `createChatInviteLink` c/ `member_limit`/`expire_date`.
- **Front:** sidebar **"Pagamentos / VIP"** (`/telegram/payments`, `IconCreditCard`), página `TelegramPayments.jsx` com abas **Planos** (CRUD: nome/bot/valor R$→centavos/descrição/grupo-canal de entrega/mensagem+mídia/ativo), **Gateway** (provedor + token mascarado + testar conexão + URL de webhook copiável + ativo) e **Vendas** (receita confirmada + lista paginada com status). `PUBLIC_API_URL` (default `https://api.bluewebchat.online`) monta a webhook URL. **Config necessária do usuário:** cadastrar o token do gateway (MP: Access Token de produção) + a URL de webhook no painel do gateway; bot admin do grupo/canal VIP.

- **Checkout como bloco de fluxo (FEITA):** bloco **"Vender plano (checkout)"** (grupo **Vendas** na paleta do construtor) + ação de botão `plan` (`buttons[].action='plan'` + `plan_id`). No `telegram-flow-runtime.sendNode`, botão `plan` vira `callback_data:pay:<plan_id>` — **reusa o handler `pay:` do runtime de pagamento**. No inspector, ação "Vender plano" mostra um `SearchSelect` dos planos do bot (`GET /telegram/payments/plans?bot_id=`). Sem mudança de schema (é JSON do botão).
- **Fixes de layout (ago/2026):** DMs e Fluxo tinham **scroll de página** (altura mal calculada) → DMs `calc(100dvh - 152px)`, canvas do Fluxo `calc(100dvh - 322px)`; separador de data das DMs virou **inline** (não flutuante — não tampa mais mensagens); balões do Fluxo **alinham no topo-esquerda** (`setViewport` com min x/y em vez de `fitView` centralizado); **arrastar bloco → inspetor abre já rolado** até a seção do bloco (via prop `focus={type,ts}` + refs de seção + scroll no container).

- **Assinatura por tempo + renovação PIX (FEITA, ago/2026):** decisão do usuário = acesso por tempo (PIX não auto-cobra). Coluna `telegram_plans.duration_days` (0 = vitalício/avulso; >0 = assinatura) + tabela `telegram_subscriptions` (migration `20260825180000_add_telegram_subscriptions`; `status` ACTIVE/EXPIRED/CANCELED, `access_until`, `group_id` snapshot do VIP, `reminded_at`, `last_payment_id`; FK bot CASCADE / plan+contact+group SET NULL). No `deliver`, se o plano tem duração → `grantSubscription` cria/**estende** a assinatura (a partir do fim atual se ainda válido, senão de agora) e a msg mostra "válido até DD/MM". **`processSubscriptions`** (cron `EVERY_10_MINUTES` no `TelegramPaymentsRunner`): (1) **expira** as vencidas → **remove o lead do grupo/canal VIP** (`banChatMember`+`unbanChatMember` = kick que permite voltar ao renovar) + DM com botão "🔓 Renovar" (`pay:<plan_id>`); (2) **lembra** 2 dias antes (`reminded_at` evita spam, 1x/dia). Renovar = tocar no botão → mesmo fluxo PIX → estende a mesma assinatura + novo invite. Endpoint `GET /telegram/payments/subscriptions` (paginado, filtros status/bot, `active_count`). Front: campo **Duração (dias, 0=vitalício)** no modal do plano + chip no card + aba **Assinantes** (contador de ativos + lista com vencimento/status). Novos helpers util: `banChatMember`, `unbanChatMember`.

### Módulo Telegram — Rodada de QA / correção de bugs visuais (ago/2026)
Auditoria de UX/UI pela extensão do navegador → lote de correções (5 agentes/eu, deploy único). **Fluxo (`TelegramFlows`+`ui/flow`):** (1) **arrastar bloco sobre um passo não funcionava** — `document.elementFromPoint` falha durante o drag nativo; troquei por **detecção geométrica** (`rf.screenToFlowPosition` + bounds `rf.getNodes()` com `n.width/height`), highlight via `querySelector('.react-flow__node[data-id]')`. (2) inspetor rola até a **última mensagem** (`lastMsgRef`) p/ blocos de mensagem/mídia e até **Botões** p/ o bloco `plan`; reseta scroll ao trocar de passo (`useEffect [node.id]`). (3) hint do canvas `nowrap`+z-index 6+borda. (4) **"Link de captação"** virou dialog dedicado (link `t.me/<bot>` + parâmetro `?start=` → `{{origem}}` + copiar), não mais o `TelegramLinkPicker` genérico. **Scroll de página (DMs/Fluxo):** a conta de altura por nº-mágico ficava curta → **medição real** (`getBoundingClientRect().top`; desconto **74** nas DMs, **96** no Fluxo por causa do padding do root); recalcula em mount/resize. **DMs:** `overscrollBehavior:'contain'` nos scrolls (fim do scroll-chaining), emoji picker com célula fixa 36px + `maxHeight` múltiplo de linha (não corta), presença/"Online" só em contato (grupo/canal não), emoji picker = `Popper`+`ClickAwayListener` (o 1º clique não é mais engolido). **Settings/BotProfile:** modal de perfil com **loading + erro + Salvar desabilitado** (evita salvar vazio e apagar o perfil real), reset de scroll ao trocar de aba, ícone de comandos `IconTerminal2` (não ⌘), texto duplicado do modal "Adicionar bot" removido, "há 3 dias" (não "3 d"). **Broadcasts/Payments/Grupos:** plurais reais (`mensagens`/`grupos`/`canais`, sem "(s)"), labels nos selects de filtro (`FormControl`+`InputLabel`), filtro em 1 linha, contagem de membros só de valores conhecidos (senão "—") e sem afirmar "0" durante loading. **Backend #13:** nome de arquivo de mídia com acento vinha mojibake (busboy decodifica latin1) → `Buffer.from(name,'latin1').toString('utf8')` no `telegram-assets.service` (forward-only). **Deixados de fora:** #15 (simplebar da sidebar global) e #20 ("copies" termo de produto).

- **Checkout configurável + "não pagou" (FEITA, ago/2026):** coluna `telegram_payment_settings.checkout_config` JSON (migration `20260825200000_add_checkout_config`) com defaults via `mergeCheckout`: `send_qr`, `send_pix_separate` (default **true** — manda o PIX copia-e-cola numa mensagem separada, fácil de copiar), `instruction_text` (vars `{plano}`/`{valor}`, HTML), `pix_message_text`, `paid_button`/`cancel_button`, `pending_text`, `expired_text`. `startCharge` monta as mensagens conforme a config (QR photo + msg separada do PIX + botões; fallback garante os botões mesmo se o QR falhar); a mensagem com os botões é a que vira "✅ Pago" no `deliver`. **"Já paguei" sem pagar** agora envia uma **mensagem** (`sendNotPaidNudge`, `pending_text` + botão "Já paguei" pra reverificar / `expired_text` + "Gerar novo PIX") — o runtime `paycheck:` chama isso em vez de só `answerCallbackQuery` (toast). **Botão "Gerar novo PIX"** (callback `pay:<plan_id>`) em todo fim sem pagamento — sem precisar de `/vip`: poller (`expireMessage`) edita a msg do PIX p/ "⌛ expirou"+botão, `cancelPayment` deixa "❌ cancelada"+botão, e o `paycheck:` numa cobrança vencida manda o `expired_text`+botão. `saveSettings` virou **update PARCIAL** (salvar "mensagens" não zera o gateway e vice-versa). `getSettings` devolve `checkout` (merge). Front: seção **"Mensagens do checkout"** no GatewayTab (toggles + textos + rótulos, botão "Salvar mensagens" próprio) + indicador "Chave salva" + resultado do "Testar conexão" fixo na tela + aviso quando pausado. Gotcha do token: `cleanToken` remove espaços/controle (preserva hífens) — um token colado com lixo/quebras estourava o header (`Cannot convert argument`). **Validado em produção:** conta MP JR..., `/users/me` 200, criar PIX 201 (QR ok).

**Próximo pendente (não iniciado):** cobrança automática no cartão (MP Preapproval) — opcional, o usuário escolheu por ora só o modelo por tempo+PIX.

### Módulo Telegram — Canais (FEITA, ago/2026)
Canais no **mesmo nível dos Grupos**, reusando a tabela `telegram_groups` (distinção por `type='channel'`). **Back:** `setWebhook` `allowed_updates` ganhou `channel_post`/`edited_channel_post`; `captureUpdate` trata `channel_post` + `chat.type==='channel'` (post anônimo → `from_name = author_signature||title`) e `my_chat_member` de canal (bot promovido a admin → descobre). `listGroups`/`listChannels` = wrappers do `listChats(orgId, botId, channel)` (filtro `type: channel ? 'channel' : {not:'channel'}`). Endpoint `GET /telegram/channels?bot_id`; **leave/invite-link/photo reusam `/telegram/groups/:id/*`** (canal É uma linha de `telegram_groups`). **Enviar no canal** = `send(kind:'GROUP', id:<linha do canal>)` (o bot posta; precisa de permissão de postar). **Front:** sidebar item **"Canais"** (`/telegram/channels`, `IconBroadcast`); a página `TelegramGroups.jsx` virou **genérica** (prop `kind='group'|'channel'` → labels/endpoint/ícone); DMs mostra seção **"Canais"** (carrega `/telegram/channels?bot_id`, chats `kind='GROUP'` + `type='channel'`, avatar/`header` mostram "Canal"); `TelegramLinkPicker` ganhou opção **"Entrar em um canal"**. **Gotcha:** bots antigos precisam do webhook re-setado p/ receber `channel_post` (fiz via script SSM `setWebhook` em todos; novos bots já vêm com isso). Descoberta do canal em si é via `my_chat_member` (já allowed).

### Módulo Telegram — Grupos (FEITA, ago/2026)
Página **`/telegram/groups`** (`views/pages/telegram/TelegramGroups.jsx`, substituiu o placeholder). Lista os grupos onde os bots da org estão (auto-descobertos). **Back:** `listGroups` (reusa `/telegram/groups?bot_id=all`) enriquecido com `bot{id,name,username,status}` + `member_count` (getChatMemberCount) + prévia. Novos endpoints em `telegram-chat.controller`: **`POST /telegram/groups/:id/leave`** (`leaveGroup`: `tg.leaveChat` + apaga a linha, mensagens em cascata) e (da leva anterior) `GET /telegram/groups/:id/invite-link` (`createChatInviteLink`, bot admin). Util `leaveChat`. **Front:** cards com **foto** (`AuthImage` via `GET /telegram/groups/:id/photo`), `@username`/privado, chip "via @bot", **contagem de inscritos**, última atividade, aviso se bot banido; ações **Mensagens** (→ DMs), **Convite** (copia link), **Sair** (confirm → leave). Filtro por bot + busca por nome. Rota em `MainRoutes`.
  - **Fase 5 — Polimento (FEITA, ago/2026):** colunas `tags` + `attributes` JSON em `telegram_contacts` (migration `20260822220000_add_telegram_contact_tags`). **Deep-link**: `/start <param>` (de `t.me/bot?start=param`) → grava `attributes.start_param` no contato; variável **`{{start_param}}`** (aliases `{{param}}`/`{{origem}}`) interpola em fluxo e automação. **Tags**: nó do fluxo (`data.apply_tags`) e passo de automação (`step.apply_tags`) aplicam tags no contato (append único, `runtime.applyTags`); aparecem como chips no header das **DMs** (`mapContact.tags`). **Humanização**: nó do fluxo tem `data.delay_seconds` (0–10) → `sendChatAction('typing')` + sleep antes de enviar. Front: `TelegramLinkPicker` ganhou opção **"Deep-link de captação"** (bot + parâmetro → gera URL); botão **"Link de captação"** no header do Fluxo (copia o link); campos **tags/atraso** no `NodeEditor` e **tags** no passo de automação. Node editor passou a usar o `TelegramLinkPicker` compartilhado (o `LinkPicker` inline virou dead-code).
  - **Fase 4 — Automações (FEITA, ago/2026):** drip/nudge temporizado após o /start (NÃO é broadcast). Tabelas **`telegram_automations`** (bot pode ter várias; `active`, `trigger_type='ON_START'`, `steps` JSON) + **`telegram_automation_enrollments`** (`@@unique(automation_id, tg_user_id)`, `next_step`, `next_send_at`, `status`, `enrolled_at`; índice `(status,next_send_at)`; FK autom+bot+contact CASCADE) — migration `20260822190000_add_telegram_automations`. **Step** = `{delay_value, delay_unit(minutes|hours|days), text, media, buttons(url-only), condition{type:'always'|'inactive', on_fail:'skip'|'stop'}}`; delay do passo 1 conta a partir do /start, seguintes após o anterior. `TelegramAutomationsService`: CRUD (`GET/POST /telegram/bots/:id/automations`, `PUT/DELETE .../:autoId`) + `enrollOnStart(bot, tgUserId)` (chamado pelo runtime no /start, independente do fluxo). **`TelegramAutomationsRunner`** (`@Cron(EVERY_MINUTE)`): pega enrollments `next_send_at<=now`, avalia condição (`inactive` = 0 msgs IN após `enrolled_at`), envia via `runtime.sendComposed` (reusa `deliver`: mídia/texto/botões URL, loga OUT nas DMs), avança ou COMPLETED; `on_fail:'stop'`→STOPPED. Runtime ganhou `deliver()` (extraído do `sendNode`) + `sendComposed()`; injeta `TelegramAutomationsService` (sem ciclo). Front: página Fluxo virou **abas Fluxo Inicial / Automações**; `TelegramAutomations.jsx` (lista + editor de passos tempo/condição/mensagem), `TelegramMediaField.jsx` (mídia+gravação reusável) e `TelegramLinkPicker.jsx` (extraído).
  - **Fase 3 — mídia + áudio (FEITA, ago/2026):** cada nó de mensagem aceita **mídia** (`data.media = {type, url, name}`; type=photo/video/voice/document) **+ texto junto** (texto vira legenda). Upload `POST /telegram/flow-assets` (multipart `{type, file}`, `FileInterceptor` memory) → `TelegramAssetsService.save`: imagem otimizada (sharp, máx 1280), **áudio convertido p/ OGG/OPUS via ffmpeg** (`ffmpeg -c:a libopus`; recorded `audio/webm`→ogg), vídeo/doc salvos como estão; grava em `uploads/telegram/` (servido por nginx/`useStaticAssets`), devolve URL pública. Runtime `sendNode`: com mídia usa `tg.sendMediaUrl` (sendPhoto/Video/Voice/Document por **URL**, caption=texto interpolado, botões=reply_markup) + `sendChatAction`; loga OUT com `media_type`/`media_file_id` (foto inline nas DMs). Front `NodeEditor`: botões Imagem/Vídeo/Doc + **Gravar áudio** (MediaRecorder→blob→upload `voice`) + Enviar áudio + preview + remover; indicador de mídia no nó. `post('/telegram/flow-assets', fd, {headers:{'Content-Type':undefined}})`.
- **OUTRAS PARTES pendentes (o usuário vai orientar — ago/2026):** abas **Grupos** (`/telegram/groups`), **Broadcasts** (`/telegram/broadcasts`). O usuário pausou p/ organizar a ideia dessas frentes. O modelo de captação de leads já decidido (ver Parte 0/1 e a conversa): campanha → link de convite com `chat_join_request` → bot admin aprova e grava lead → DM de boas-vindas (opt-in por bot). Broadcasts segmentados por grupo + geral (dedupe por `tg_user_id`), fila reusando BullMQ. Já existem as tabelas `telegram_contacts`/`telegram_groups`/`telegram_messages` (Parte 2) como base; faltam `telegram_broadcasts`/`telegram_sends` e o handler de `chat_join_request` no webhook.
- **Parte 0 — grupo na sidebar (FEITA):** novo grupo **"Telegram"** logo **abaixo de Email Marketing** (`front/.../menu-items/telegram.js`, registrado em `menu-items/index.js` na posição `[dashboard, webchat, quizzes, emails, telegram, admin, settings]`). Ícone do grupo = **`IconBrandTelegram`** (Tabler).
- **Parte 0.1 — 4 itens placeholder (FEITA):** o grupo Telegram tem 4 itens (ainda **vazios/em construção**): **Configurações** (`/telegram/settings`, `IconAdjustmentsHorizontal`), **Grupos** (`/telegram/groups`, `IconUsersGroup`), **Broadcasts** (`/telegram/broadcasts`, `IconSpeakerphone`), **DMs** (`/telegram/dms`, `IconMessages`). Rotas em `routes/MainRoutes.jsx` (bloco `path:'telegram'`) renderizam **`views/pages/telegram/TelegramPlaceholder.jsx`** — página "Em construção" reusável (ícone Telegram em gradiente `#2AABEE→#229ED9`, título+descrição, chip; theme-aware via tokens/`alpha`), recebendo `title`/`description`/`Icon` por prop.
- **Densidade da sidebar (ago/2026):** itens de grupo estavam altos demais. `NavItem` (`layout/MainLayout/MenuList/NavItem/index.jsx`): `mb 0.5→0.25` e adicionado `py: 0.5` no caso item-aberto nível 1 (antes não tinha `py` → herdava o padding default gordo do `ListItemButton`).
- **Parte 1 — Configurações / cadastro de Bots (FEITA, ago/2026):** primeira tela com back real do módulo.
  - **Modelo de resiliência (decidido com o usuário):** o sistema entende **"Bots"** (credencial = **token** do @BotFather; **nome** = apelido no Pub Mail). Criar bots em **contas/números diferentes** é responsabilidade do usuário (fora do sistema) — é o que protege contra ban (ban de conta derruba todos os bots dela; ban de bot só aquele; opt-in de DM é **por bot**). **Excluir bot = cascade** de tudo vinculado (grupos/links/broadcasts/DMs); **Opção 1** p/ leads: apaga só o **vínculo**, lead órfão é removido, lead que também é de outro bot **permanece**; **broadcast geral preservado**. Bot banido → **aviso no front** nas telas relacionadas (não apaga, só sinaliza via `status`).
  - **Back (`back/src/telegram/`):** tabela **`telegram_bots`** (migration `20260821130000_add_telegram_bots`; org-scoped, `@@unique(organization_id, tg_bot_id)`, enum `telegram_bots_status` ACTIVE/BANNED/ERROR, `webhook_secret`, `last_error`, `last_checked_at`). **GOTCHA da migration:** FK falhou (erro 3780) porque o `CREATE TABLE` sem `COLLATE` pegou o default do MySQL 8 (`utf8mb4_0900_ai_ci`); o banco é **`utf8mb4_unicode_ci`** → toda tabela nova precisa de `DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci` senão a FL p/ `organizations.id` não casa. (resolver com `prisma migrate resolve --rolled-back <nome>` antes de reaplicar.)
  - `telegram-api.util.ts` = helpers finos da Bot API (fetch nativo + AbortController 12s): `getMe` (valida token + puxa `@username`/nome; 401/404 ⇒ `unauthorized`), `setWebhook` (com `secret_token` + allowed_updates incl. `chat_join_request`/`chat_member`), `deleteWebhook`. `TelegramBotsService`: `create` (valida getMe → cria → seta webhook `${PUBLIC_API_URL||https://api.bluewebchat.online}/telegram/webhook/:botId`), `revalidate` (getMe → atualiza status; 401 ⇒ BANNED, rede instável não marca banido), `update` (nome e/ou troca de token), `remove` (deleteWebhook best-effort + delete). **Token nunca volta cru** — `toResponse` devolve `token_masked` (`123456:••••XXXX`) + status/webhook/counts(0 por ora). Endpoints (JWT, `/telegram/bots`): GET, POST, PATCH `:id`, POST `:id/revalidate`, DELETE `:id`. **Webhook público** `POST /telegram/webhook/:botId` (`telegram-webhook.controller`, sem JWT): confere o header `x-telegram-bot-api-secret-token` vs `webhook_secret`, **ACK 200 sempre** e no-op nesta fase (processamento de updates pluga nas próximas partes). Módulo `TelegramModule` registrado no `app.module`.
  - **Mensagem de teste (FEITA):** coluna `test_chat_id BIGINT` (migration `20260821160000_add_telegram_bot_test_chat`) guarda o **chat privado** que falou com o bot. O webhook (`captureFromUpdate`) grava esse chat_id quando chega `message.chat.type==='private'` — ou seja, quando o **dono manda `/start`** no próprio bot (restrição do Telegram: bot não inicia conversa nem conhece o telefone do dono). Endpoint `POST /telegram/bots/:id/test` (`sendTest`) manda uma DM de teste pra esse chat; se `test_chat_id` nulo → `BadRequest` instruindo a dar `/start`; `403` → limpa o chat e pede novo `/start`. `sendMessage` no `telegram-api.util`. `toResponse` inclui `can_test`. Front: botão **"Enviar mensagem de teste"** em cada card (tooltip explica o pré-requisito do `/start`).
  - **Front (`views/pages/telegram/`):** `TelegramSettings.jsx` (rota `/telegram/settings`, substituiu o placeholder) — 2 abas: **Cadastro de Bots** (grid de cards: avatar, nome, `@username`, token mascarado, chip de status, saúde do webhook, contadores, `Alert` de aviso quando banido/instável, ações Revalidar/Editar/Excluir; dialog add/edit; confirm de exclusão explicando o cascade) e **Ajuda** (reformulada: explica que o BotFather é um **chat com botões**, não painel; guia pelo menu **`/mybots`** com **pílulas visuais `Tap`** imitando os botões e um componente **`Flow`** que mostra o caminho exato de toques — ex.: `/mybots › @seu_bot › Bot Settings › Group Privacy › Turn off` e `… › Allow Groups? › Enabled`; + criar/personalizar via `Edit Bot`; + "Atalhos por comando" (reference `BOTFATHER_COMMANDS`) + dica de resiliência). `TelegramLogo.jsx` = SVG oficial do Telegram (avião no círculo azul). Theme-safe (Chip `color`, `Alert severity`, tokens `background.paper/default/divider`). **Aviso de bot banido nas telas Grupos/DMs/Broadcasts** virá quando essas telas existirem (o `status` já está pronto pra ler).
- **Parte 2 — DMs / Chat (FEITA, ago/2026):** tela de chat estilo Telegram (`views/pages/telegram/TelegramDMs.jsx`, rota `/telegram/dms`) — serve p/ testar DMs, broadcasts e mensagens de grupo.
  - **Modelo (migration `20260821190000_add_telegram_chat`):** `telegram_contacts` (lead que falou em DM: `bot_id` FK→telegram_bots **ON DELETE CASCADE**, `tg_user_id`, nomes, `username`, `last_message_*`, `@@unique(bot_id, tg_user_id)`), `telegram_groups` (grupo auto-descoberto: `tg_chat_id`, `title`, `@@unique(bot_id, tg_chat_id)`, FK cascade), `telegram_messages` (`chat_kind` CONTACT|GROUP, `contact_id`/`group_id` FK cascade, `direction` IN|OUT, `text`, `media_type`, `media_file_id`, `from_name`, `created_at` DATETIME(3)). Enums `telegram_msg_direction`, `telegram_chat_kind`. **Excluir bot → cascade** apaga contatos/grupos/mensagens (via FK).
  - **Back (`telegram-chat.service`/`controller`):** o **webhook** agora chama `captureUpdate` — DM privada → upsert contato + grava msg IN + mantém `test_chat_id`; grupo → upsert grupo + msg IN (com `from_name`); `my_chat_member` → descobre grupo; mídia vira rótulo (`📷 Foto`, etc). Endpoints (JWT, org-scoped): `GET /telegram/conversations?bot_id&q&page` (paginado **30**, busca server-side por @/número/nome), `GET /telegram/groups?bot_id`, `GET /telegram/messages?kind&id&since` (`since`=ISO p/ polling incremental; devolve `server_time`), `POST /telegram/messages` (**multipart**, texto e/ou foto — `FileInterceptor('photo')` memory; texto→`sendMessage`, foto→`sendPhoto`), `GET /telegram/contacts/:id/photo` e `GET /telegram/messages/:id/media` (**proxy binário** — `getUserProfilePhotos`/`getFile`/`downloadFile`; token fica no server; `Cache-Control` 1 dia; 404 se não houver). Util ganhou `getUserProfilePhotos/getFile/downloadFile/sendPhoto`.
  - **Configurar bot (modal, FEITA):** engrenagem no card (`BotProfileDialog.jsx`) com abas **Configurações** (nome/`setMyName`, sobre/`setMyShortDescription`, descrição/`setMyDescription`, comandos/`setMyCommands` — editor add/remove; contadores reais no card via `groupBy`; foto do bot via proxy `GET /telegram/bots/:id/photo`, `getUserProfilePhotos`(tg_bot_id)→getChat fallback; **status** entrar-em-grupos/privacidade com ✓/⚠ relativo ao fluxo) e **Preview** (réplica da tela de abertura do Telegram: foto+nome+`@`+card "O que este bot faz?"=descrição+botão INICIAR). Endpoints `GET/PATCH /telegram/bots/:id/profile` (getMyName/Description/ShortDescription/Commands + getMe p/ can_join_groups/can_read_all_group_messages). **Limitação:** foto do bot e privacidade/entrar-em-grupos **só o BotFather** altera (Bot API não seta foto do próprio bot nem privacidade) — o modal mostra e orienta. `AuthImage.jsx` extraído p/ componente compartilhado (DMs + Settings). Prévia da conversa mostra o autor: grupo grava `Fulano:`/`Você:` no `last_message_text`; DM mostra `Você:` no front quando `last_message_dir==='OUT'`.
  - **Front:** layout 2 colunas. Esquerda: **Select de bot** + busca (debounce 350ms, server-side) → seção **Grupos** + **Conversas** paginadas (30, "Carregar mais"), avatar com **foto real** via `AuthImage` (busca o proxy como **blob autenticado → objectURL**, cache em `Map` — `<img src>` puro daria 401). Direita: chat com balões IN/OUT, **imagens inline** (mesmo `AuthImage`), header com **relógio ao vivo (segundos)** + "sync HH:MM:SS", **emoji popover** próprio (sem dep nova), **anexar foto** (preview + enviar), Enter envia. **"Tempo real" = polling** (mensagens do chat aberto a cada 3,5s via `since`; lista a cada 10s). **Gotcha:** enviar foto usa `FormData` com `post(url, fd, { headers: { 'Content-Type': undefined } })` (deixa o browser pôr o boundary); texto-só vai como JSON (o `FileInterceptor`/multer ignora requisições não-multipart).
  - **Ícone por GRUPO (novo padrão):** os headers de grupo da sidebar não tinham ícone. Adicionado campo **`icon`** no objeto de grupo (dashboard=`IconLayoutDashboard`, webchat=`IconBrandHipchat`, quizzes=`IconListLetters`, email=`IconMailBolt`, telegram=`IconBrandTelegram`, admin=`IconUserShield`, settings=`IconSettings`) e o `NavGroup` (`layout/MainLayout/MenuList/NavGroup/index.jsx`) renderiza `item.icon` (17px, stroke 1.9, cor `inherit`, `gap:1`) antes do título no header do accordion. Grupo sem `icon` continua funcionando (render condicional). No modo mini (drawer fechado) o header não aparece — só os ícones dos itens, igual antes.

### Módulo Telegram — Hardening anti-flood + botão copiar PIX + fix grupos (ago/2026)
Rodada de correção após bug real de produção: **"broadcasts não enviaram" + bot "digitando infinitamente"**.
- **Causa raiz (flood-ban do Telegram):** o bot levou `429 Too Many Requests: retry_after ~16000s` (~4,5h!). Origem: a **mensagem rotativa de DM** rodava o sweep **a cada 3s** (tick), ignorava o `interval`, e — pior — quando o `editMessageText` dava 429 ela **reenviava** (`sendMessage`) → **amplificava** o flood. Junto, o **worker de broadcast** também martelava: o util `telegram-api.util.call()` **não expunha `parameters.retry_after`**, então o `sendOne` usava fallback de **3s** e re-tentava a cada 3s (cada tentativa ainda mandava `sendChatAction('typing')` → o "digitando infinito"). Tudo isso mantinha o `retry_after` sempre alto (renovava o castigo).
- **Fixes (back):**
  1. **`telegram-api.util.ts`:** `TgResult` ganhou `parameters?: { retry_after? }` e o `call()` agora devolve `json.parameters` no erro. **Sem isso o retry_after real era invisível.**
  2. **`telegram-broadcasts.runner.ts`:** 429 agora honra o **retry_after REAL** (`min(retry_after, 6h)`, fallback 30s) em vez de 3s. Adicionado `cooling(botId)` + **filtro no loop**: sends de bot em flood-wait ficam PENDING e são **pulados** (polling barato de 2,5s) em vez de **bloquear o worker** (antes 1 bot floodado travava todos por horas) e sem re-hammer. `sendOne` checa `cooling` **antes** do `typing`/envio (mata o "digitando").
  3. **`telegram-rotators.runner.ts`:** `botCooldown` por bot + `note429()` que respeita o retry_after e **NÃO reenvia** em 429 (era o amplificador). Cadência mínima segura: **DM 30s** (`DM_MIN_INTERVAL`), **grupo/canal 10s** (`GROUP_MIN_INTERVAL`) — floor no runner, independente do que o usuário setar (o input aceita 5s, mas o runner protege). `dmSweep` gated por `eff` (não roda mais a cada 3s). `sleep(150)` entre contatos (~6-7 msg/s).
- **Teto de edições por lead na rotativa de DM (FEITA, ago/2026):** pra nunca mais floodar por edição excessiva de um mesmo lead. Tudo em `telegram_contacts.attributes` (sem migration): (1) **teto rígido `EDITS_PER_HOUR=10`** por lead numa **janela deslizante de 1h** (`rotator_window`/`rotator_edits`); (2) **espaçamento mínimo `MIN_EDIT_GAP` (~6 min = 3600/10)** entre edições do mesmo lead (`rotator_last_edit`) → distribui as ≤10/h ao longo da hora em vez de 10 seguidas; (3) **detecção de lead morto** (`isDeadError`: 403/"chat not found"/"bot was blocked"/"user is deactivated"/etc.) → marca `attrs.rotator_dead=true` e **PARA de editar** aquele lead (não fica editando pra sempre msg de quem saiu do Telegram). Só volta via **`bumpForContact`** (interação do lead = está vivo → limpa `rotator_dead`). Bump seta `rotator_last_edit` (não edita logo depois) e **não conta** no teto (é evento do usuário, não loop). Grupo/canal (`rotate`): guarda `isDeadError` → não fica reenviando se o bot foi expulso.
  - **Recuperação:** flood-ban só passa com **tempo** (decai sozinho quando paramos de martelar — observado 16128→14655s). Nada envia (broadcast/rotativa/pagamento) até limpar. Quando o cooldown em memória expira, os runners voltam sozinhos. **Diagnóstico:** `curl -s https://api.telegram.org/bot$TOK/sendMessage` server-side (token via DB, NUNCA no chat) → o `retry_after` no 429 é o tempo restante. Emergência: `UPDATE telegram_rotators SET active=0` pra estancar na hora.
- **Botão "Copiar código" no PIX (FEITA):** no `startCharge`, o código PIX agora vai em bloco **`<pre>`** (o Telegram renderiza um "copiar" nativo na própria bolha) + um botão **`copy_text`** (Bot API 8.0) rotulável que copia ao toque. Campo `copy_button_text` no `DEFAULT_CHECKOUT`/`mergeCheckout` (vazio = sem botão) e no front (GatewayTab → "Botão de copiar o código PIX"). Vive no JSON `checkout_config` (sem migration).
- **Bug visual — grupos "vazando pra lateral" (FEITA):** o grid de `TelegramGroups.jsx` usava `repeat(N, 1fr)`; trilhas `1fr` têm `min-width:auto` = min-content do card, e a **prévia `noWrap`** longa estourava a trilha pra fora do container. Fix: **`minmax(0, 1fr)`** (trava a trilha e deixa o `noWrap` truncar). Padrão pra qualquer grid com filho `noWrap`.
- **Aviso de flood no card do bot (FEITA):** coluna `telegram_bots.flood_until DATETIME(3)` (migration `20260826150000_add_bot_flood_until`) = até quando o Telegram barra envios. Helper `telegram-flood.util.ts` (`noteFlood`/`clearFlood`, best-effort): os runners de **broadcast** e **rotativa** gravam `flood_until` no 429 (via `noteFlood`) e limpam ao voltar a enviar (`clearFlood`/`onOk`, throttle via cooldown em memória). `toResponse` do bots devolve `flood_until` (ISO) + `flooded` (bool). Front (`TelegramSettings.jsx`): pill **"Em pausa"** (warning) + `Alert` "⏳ Pausa TEMPORÁRIA do Telegram — volta sozinho em ~Xh (por volta de HH:MM)"; texto do BANNED clarificado como **permanente** (token revogado, não volta sozinho — recadastrar). Botão de teste desabilitado durante flood. `floodEta()` calcula o relativo/horário no front; `flooded` se auto-limpa quando `flood_until` passa (sem depender de write). **Distinção:** `status=BANNED` (getMe 401 = permanente) vs `flood_until>now` (429 = temporário/auto-recupera). **Diagnóstico/manual:** setar `flood_until` na mão = `UPDATE telegram_bots SET flood_until=DATE_ADD(UTC_TIMESTAMP(3),INTERVAL <retry> SECOND)`.
- **Gotcha de DEPLOY (IMPORTANTE):** passar a **URL pré-assinada do S3 inline** no `--parameters` do SSM **falha silenciosamente** — os `&`/`%` da URL quebram o parser shorthand do `aws ssm`, o `curl` não baixa, o `tar` não extrai, mas o `nest build` recompila o código VELHO e o `test -f dist/main.js` passa → parece que deployou (mostrou BUILD_OK) mas **não deployou**. Sintoma: `grep marcador src` = 0, mtime do `src` antigo. **Solução:** passar a URL via **base64** (`echo <b64> | base64 -d > /tmp/url.txt` na instância, depois `curl $(cat /tmp/url.txt)`), e **sempre validar** com `grep -c <marcador> <arquivo>` no `src` E no `dist` antes/depois do build.

## Convenções / lembretes
- **DRIFT servidor↔local (IMPORTANTE, set/2026):** o servidor tinha código que o clone local NÃO tinha (feature "welcome"/`sendWelcomeToLead` de 20/08 — `schedules.service.ts`, `leads.service.ts`, `quizzes.service.ts`, `project.*`, `analytics.service.ts`, `schedules.module.ts`). Um deploy do local **sobrescreveu** `schedules.service.ts` e apagou o método (reconstruído depois; os outros 5 foram puxados pro local). **ANTES de deployar um arquivo, conferir se o servidor tem versão mais nova**: `find /opt/pub-mail/back/src -newer <ref>` ou comparar `md5sum` local × servidor; se diferir, baixar o do servidor primeiro (SSM `base64 -w0 tar` cabe até ~18KB por chamada). **E o `nest build` EMITE dist mesmo com erro de TypeScript** (o `test -f dist/src/main.js` passa!) → no deploy, grepar a saída por `error TS|Found N error` e **não reiniciar** se houver.
- **E-mail de boas-vindas (welcome) — existe e é backend-only:** `email_projects.settings.welcome = {enabled, templateId}`; endpoints `GET/PUT /email/projects/:id/welcome`; `EmailSchedulesRunner.sendWelcomeToLead(projectId, leadId)` (best-effort, cobra token, grava sent) é chamado com `void` em `leads.service.publicSubscribe` e `quizzes.service.captureLead` quando o vínculo lead→projeto é NOVO. Sem UI no front.
- **README (set/2026):** reescrito com todos os módulos (`README.md` PT + `README.en.md` EN, seletor de idioma no topo — GitHub não roda JS, então é link, não botão) e imagens em `docs/images/` (dashboard, telegram-dms, integracoes). Ao criar módulo novo, atualizar os DOIS READMEs. `docker-compose.yml` é só infra de dev (MySQL/Redis/phpMyAdmin) — módulos novos não exigem serviço novo nele.
- **Contexto novo/recarregado ⇒ reler CLAUDE.md + memória + código da área da task ANTES de agir** (ver aviso no topo).
- **Ao concluir cada desenvolvimento, ATUALIZAR este CLAUDE.md** (seção da área + marco de evolução recente) e a memória — o usuário pediu explicitamente que o conhecimento fique persistido pra outra sessão continuar. Documentar: tabelas/migrations novas, colunas, endpoints, arquivos-chave, gotchas e como testar.
- Deploy front sempre com **build-em-temp + swap atômico** (não buildar direto em `dist`).
- Build back: o processo em memória continua de pé durante o `nest build`; só reiniciar se o build passou.
- `personalizacao` do webchat é **JSON livre** (`settings: Record<string,any>` no DTO) → campos novos persistem sem mexer no back. **Anúncios NÃO** — têm tabela/DTO próprios (`webchat_ads` / `update-webchat-ads.dto.ts`), exigem coluna+DTO+service+payload.
- NestJS tem `ValidationPipe` global com `forbidNonWhitelisted` → campo novo em payload com DTO estruturado precisa entrar no DTO senão é descartado/rejeitado.
- Multi-tenancy: scoping manual por `organization_id` (vindo de `req.user.organizationId`) em cada service. Métodos admin ignoram esse scoping (são cross-org, atrás do RolesGuard).
